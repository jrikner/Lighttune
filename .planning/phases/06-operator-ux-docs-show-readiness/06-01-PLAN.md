---
phase: 06-operator-ux-docs-show-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - sekonic-bridge/README.md
  - data/config.json.example
  - lua/config.lua
  - lua/SekonicCalibrator.lua
  - tests/test_config.lua
  - tests/run.lua
autonomous: true
requirements:
  - TOP-01
  - UX-05
must_haves:
  truths:
    - macOS onPC + local bridge runbook appears before Pi sections in README and sekonic-bridge/README.md (D-101, D-103)
    - Runbook covers venv, pip, Homebrew libusb, server.py, curl /status, plugin Bridge Status (D-104)
    - data/config.json.example bridge_ip is 127.0.0.1 (D-102)
    - sekonic-bridge README config path is plugin root config.json; no socket.http or github_token claims
    - lua/config.lua exports get_sep, get_plugin_dir, get_data_dir, load_config
    - load_domain_modules registers config module (D-105, D-108)
    - tests/test_config.lua covers path helpers and config regex parse (D-120)
    - lua5.4 tests/run.lua green after extraction
  artifacts:
    - lua/config.lua
    - tests/test_config.lua
  key_links:
    - from: README.md
      to: data/config.json.example
      via: bridge_ip 127.0.0.1
      pattern: 127\.0\.0\.1
    - from: SekonicCalibrator.lua
      to: config.lua
      via: domain.config or local config_mod = domain.config
      pattern: config%.load_config|domain%.config
---

<objective>
Publish macOS onPC + local sekonic-bridge as the primary deployment path, fix config/doc drift, and extract `config.lua` before UI modularization.

Purpose: Satisfies TOP-01 and UX-05 (macOS runbook first); establishes Wave 1 config module per D-105.
Output: macOS runbook sections, corrected config.json.example, config.lua module with host tests, doc grep gates passing.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operator-ux-docs-show-readiness/06-CONTEXT.md
@.planning/phases/06-operator-ux-docs-show-readiness/06-RESEARCH.md
@.planning/phases/06-operator-ux-docs-show-readiness/06-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@README.md
@sekonic-bridge/README.md
@data/config.json.example
@lua/SekonicCalibrator.lua
@tests/run.lua
</context>

<artifacts_this_phase_produces>
- `README.md` — new **macOS + onPC + local bridge** section before Pi/VLAN content; topology diagram with 127.0.0.1; plugin root config.json path
- `sekonic-bridge/README.md` — macOS install runbook first; Pi stage-split second; corrected config path; TCP/LuaSocket wording (not socket.http); remove github_token/community_upload references
- `data/config.json.example` — `bridge_ip: "127.0.0.1"`
- `lua/config.lua` — get_sep, get_plugin_dir, get_data_dir, load_config; optional pure `normalize_plugin_dir(base, host_os, sep)` for host tests
- `tests/test_config.lua` — path join + regex parse assertions
- Updated `tests/run.lua` — includes test_config runner
- Monolith delegates config I/O to config module; inline Section 5 config functions removed
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: macOS primary runbook + doc drift fixes (TOP-01, UX-05, D-101–D-104)</name>
  <files>README.md, sekonic-bridge/README.md, data/config.json.example</files>
  <read_first>06-CONTEXT.md, 06-RESEARCH.md (macOS runbook table), README.md, sekonic-bridge/README.md, data/config.json.example</read_first>
  <action>
    Add a **macOS + GrandMA3 onPC + local sekonic-bridge** section to README.md BEFORE any Pi/VLAN deployment section (D-103). Document: Homebrew `brew install libusb`; Python 3.10+ venv in sekonic-bridge/; `pip install -r requirements.txt`; pyusb backend verify command; mock path `python server.py --mock --host 127.0.0.1 --port 8765`; real C-7000 path with USB plug; `curl -s http://127.0.0.1:8765/status`; copy config.json.example to plugin root with bridge_ip 127.0.0.1; plugin Bridge Status verification (D-104). Include Apple Silicon troubleshooting: prefer Homebrew Python; optional `export DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib` if pyusb reports No backend available. Reorder sekonic-bridge/README.md the same way — macOS section first, Pi stage-split second with VLAN/firewall/troubleshooting subsection retained. Fix sekonic-bridge/README.md: config path must be plugin root `config.json` (same folder as plugin.xml), NOT data/config.json; replace all `socket.http` references with HTTP/1.0 over LuaSocket TCP; remove github_token and community_upload config keys. Update data/config.json.example bridge_ip from 192.168.1.50 to 127.0.0.1 (D-102). Update README config example block to match 127.0.0.1. Keep Pi content as secondary — do not delete stage-split instructions.
  </action>
  <verify>
    <automated>grep -n '127.0.0.1' README.md sekonic-bridge/README.md data/config.json.example && grep -n -i 'macOS' README.md | head -1 | awk -F: '{print $1}' | xargs -I{} sh -c 'grep -n -i "Pi\|Raspberry" README.md | head -1 | awk -F: "{if (\$1+0 > {}+0) exit 0; else exit 1}"' && ! grep -q 'socket\.http' sekonic-bridge/README.md && ! grep -q 'github_token' sekonic-bridge/README.md && ! grep -q '192.168.1.50' data/config.json.example</automated>
  </verify>
  <acceptance_criteria>
    - macOS section appears before Pi section in both README files (D-103)
    - config.json.example uses 127.0.0.1 (D-102)
    - sekonic-bridge README documents plugin root config.json path
    - No socket.http or github_token in sekonic-bridge README
    - Pi stage-split runbook still present as secondary path
  </acceptance_criteria>
  <done>TOP-01 and UX-05 doc deliverables published; config example aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Doc grep audit gate script (TOP-01, UX-05)</name>
  <files>README.md, sekonic-bridge/README.md, data/config.json.example</files>
  <read_first>06-VALIDATION.md (per-task verification map), 06-RESEARCH.md (Validation Architecture doc grep rows)</read_first>
  <action>
    Run and document (in plan SUMMARY) the Phase 6 doc grep audit commands: (1) `grep -n '127.0.0.1' README.md sekonic-bridge/README.md data/config.json.example` — all three must match; (2) `grep -n -i 'macOS' README.md` — must return at least one hit in macOS runbook section; (3) `grep -n 'data/config.json' sekonic-bridge/README.md` — must return zero hits (wrong path removed); (4) README must not claim community GitHub upload from console or disk GDTF read. Fix any failing grep before marking task done. No false claims per BASE-02 carry-forward.
  </action>
  <verify>
    <automated>grep -q '127.0.0.1' README.md && grep -q '127.0.0.1' sekonic-bridge/README.md && grep -q '127.0.0.1' data/config.json.example && ! grep -q 'data/config.json' sekonic-bridge/README.md</automated>
  </verify>
  <acceptance_criteria>
    - All four doc grep checks pass
    - 127.0.0.1 present in README, sekonic-bridge README, and config example
    - Wrong data/config.json path absent from sekonic-bridge README
  </acceptance_criteria>
  <done>Doc grep gate green for Wave 1.</done>
</task>

<task type="auto">
  <name>Task 3: Extract config.lua + host tests (D-105, D-108, D-120)</name>
  <files>lua/config.lua, lua/SekonicCalibrator.lua, tests/test_config.lua, tests/run.lua</files>
  <read_first>lua/SekonicCalibrator.lua (get_sep L1364–1421, load_domain_modules L18–48), 02-CONTEXT.md D-23, 06-RESEARCH.md (config extraction map), tests/lib_assert.lua</read_first>
  <action>
    Create lua/config.lua returning module table M with: get_sep() (GetPathSeparator pcall fallback "/"), get_plugin_dir() (GetPath PluginLibrary + HostOS fallback — move verbatim from monolith L1373–1396), get_data_dir(), load_config() (regex parse github_username, bridge_ip, bridge_port default 8765 — move verbatim L1407–1421). Add pure helper normalize_plugin_dir(base, host_os, sep) returning expected plugin root path for Windows vs macOS/Linux — used only by host tests, not MA3 runtime. Extend load_domain_modules() return table with config = try_require("config") (D-108). Replace monolith calls: local config_mod = domain.config; use config_mod.load_config(), config_mod.get_plugin_dir(), etc. Remove inline get_sep/get_plugin_dir/get_data_dir/load_config from SekonicCalibrator.lua. Keep save_fixture_log_local in monolith for now (uses get_data_dir via config module). Create tests/test_config.lua: assert normalize_plugin_dir for macOS and Windows paths; assert load_config regex extracts bridge_ip and default port 8765 from fixture JSON strings (use temp string input — extract parse logic as config.parse_config_content(content) if needed for testability without io.open). Register test_config in tests/run.lua. Run lua5.4 tests/run.lua — must pass (139+ baseline + new tests).
  </action>
  <verify>
    <automated>test -f lua/config.lua && grep -q 'load_config' lua/config.lua && grep -q 'config' lua/SekonicCalibrator.lua && test -f tests/test_config.lua && lua5.4 tests/run.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - config.lua exists with all four path/config functions
    - load_domain_modules registers config module (D-105, D-108)
    - Monolith no longer defines inline load_config/get_plugin_dir
    - tests/test_config.lua added and run.lua includes it
    - Host test suite green (D-120)
  </acceptance_criteria>
  <done>Wave 1 complete — config module extracted; docs and example aligned to 127.0.0.1.</done>
</task>

</tasks>

<verification>
- macOS runbook before Pi in README and sekonic-bridge/README.md
- config.json.example bridge_ip is 127.0.0.1
- Doc grep audit passes (127.0.0.1, no data/config.json in bridge README)
- lua/config.lua extracted; tests/test_config.lua green
- lua5.4 tests/run.lua regression green
</verification>

<success_criteria>
- TOP-01: macOS + onPC + local bridge at 127.0.0.1 documented as primary path
- UX-05: macOS runbook first; Pi stage-split second
- D-105: config.lua extracted before UI split
</success_criteria>

<output>
Create `.planning/phases/06-operator-ux-docs-show-readiness/06-01-SUMMARY.md` when done
</output>
