---
phase: 06-operator-ux-docs-show-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - lua/patch_api.lua
  - lua/fixture_apply.lua
  - lua/SekonicCalibrator.lua
autonomous: true
requirements:
  - UX-01
  - UX-02
  - CAL-04
must_haves:
  truths:
    - goals module imported as goal_eval; session table named session_goals — no shadowing (Wave 0, D-109)
    - show_assessment and auto-loop call goal_eval.goal_status_str and goal_eval.goals_met (not session table)
    - lua/patch_api.lua exports get_fixture_from_patch and read_capabilities_from_patch
    - lua/fixture_apply.lua exports select_group, apply_color_xyY, apply_color_hsb, calibrate_group
    - calibrate_group uses SetColor xyY first with HSB fallback; Y=1.0 brightness preserved (CAL-04, D-114–D-116)
    - load_domain_modules registers patch_api and fixture_apply (D-106, D-108)
    - Monolith Sections 3b/4 functions removed; call sites delegate to modules
    - lua5.4 tests/run.lua green after extraction (D-120)
  artifacts:
    - lua/patch_api.lua
    - lua/fixture_apply.lua
  key_links:
    - from: patch_api.lua
      to: DataPool Groups FixtureType
      via: read_capabilities_from_patch
      pattern: DataPool|FixtureType
    - from: fixture_apply.lua
      to: SetColor
      via: calibrate_group xyY then HSB fallback
      pattern: SetColor.*xyY|apply_color_hsb
    - from: SekonicCalibrator.lua
      to: goal_eval
      via: renamed module import (not goals)
      pattern: goal_eval%.goals_met|local goal_eval
---

<objective>
Fix goals module shadowing (Wave 0), then extract patch read and fixture apply layers from the monolith.

Purpose: Satisfies UX-01/UX-02 (patch make/model + capability hints) and CAL-04 (SetColor apply); unblocks Wave 3 UI split per D-106.
Output: patch_api.lua, fixture_apply.lua, shadowing bug resolved, host tests still green.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operator-ux-docs-show-readiness/06-CONTEXT.md
@.planning/phases/06-operator-ux-docs-show-readiness/06-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@lua/SekonicCalibrator.lua
@lua/goals.lua
@lua/color_math.lua
@tests/run.lua
</context>

<artifacts_this_phase_produces>
- `lua/patch_api.lua` — get_fixture_from_patch(group_name), read_capabilities_from_patch(group_name) moved verbatim from monolith L306–330, L1229–1310
- `lua/fixture_apply.lua` — select_group, apply_color_xyY, apply_color_hsb, calibrate_group moved verbatim from L1316–1347
- Renamed imports: `local goal_eval = domain.goals`; session variable `session_goals` throughout main/calibration paths
- Updated load_domain_modules with patch_api and fixture_apply entries
- Grep guard: no `local goals = get_session_goals` without renaming; no `goals.goals_met` where goals is session table
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Fix goals module vs session_goals shadowing (Wave 0, D-109)</name>
  <files>lua/SekonicCalibrator.lua</files>
  <read_first>lua/SekonicCalibrator.lua (L53 goals import, L540 show_assessment, L1489 main goals=, L1628 goals_met), lua/goals.lua, 06-RESEARCH.md (Pattern 2)</read_first>
  <action>
    Rename module import at module scope: change `local goals = domain.goals` to `local goal_eval = domain.goals`. Keep QUALITY/GOAL_MAX/GOAL_MIN/GOAL_SKIP aliases from goal_eval. In show_assessment: rename parameter `goals` to `session_goals`; update all references inside function; call goal_eval.goal_status_str and goal_eval.goals_met explicitly (never session_goals.goals_met). In get_session_goals return path and all callers: rename local `local goals = get_session_goals(...)` to `local session_goals = get_session_goals(...)`; propagate session_goals through main inner/outer loops, get_measurement_params, apply_historical_prefill, show_session_summary, goals_summary_line calls. Update goals_summary_line parameter name to session_goals for clarity. At auto-loop L1628 replace goals.goals_met(measured, goals) with goal_eval.goals_met(measured, session_goals). Grep entire file: zero occurrences of session table calling .goal_status_str or .goals_met; zero reassignment `local goals =` that shadows goal_eval after import. Do NOT change MessageBox strings or goal evaluation logic — rename only (D-109).
  </action>
  <verify>
    <automated>grep -q 'local goal_eval' lua/SekonicCalibrator.lua && grep -q 'goal_eval\.goals_met' lua/SekonicCalibrator.lua && grep -q 'goal_eval\.goal_status_str' lua/SekonicCalibrator.lua && ! grep -q 'local goals = get_session_goals' lua/SekonicCalibrator.lua && lua5.4 tests/run.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Module imported as goal_eval; session data as session_goals
    - show_assessment uses goal_eval.goal_status_str(measured, session_goals.cri) pattern
    - Auto-loop uses goal_eval.goals_met(measured, session_goals)
    - No goals shadowing remains in monolith
    - Host tests still green (D-120)
  </acceptance_criteria>
  <done>Wave 0 shadowing gap closed before further extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Extract patch_api.lua (UX-01, UX-02, D-106, D-114, D-115)</name>
  <files>lua/patch_api.lua, lua/SekonicCalibrator.lua</files>
  <read_first>lua/SekonicCalibrator.lua (get_fixture_from_patch L306–330, read_capabilities_from_patch L1229–1310), 06-RESEARCH.md (function migration map), ARCHITECTURE.md patch_api section</read_first>
  <action>
    Create lua/patch_api.lua returning module M. Move get_fixture_from_patch(group_name) verbatim — reads DataPool Groups, returns make/model from FixtureType with manual fallback path preserved at call site in get_fixture_model_input (leave get_fixture_model_input in monolith for Wave 3). Move read_capabilities_from_patch(group_name) verbatim — detects has_tint, has_ctb, has_cto, has_color_wheel, gdtf_cri, gdtf_cct from DMXModes/Attributes. No MessageBox in patch_api (D-106 integration layer only). Extend load_domain_modules with patch_api = try_require("patch_api"). Replace monolith calls: patch_api.get_fixture_from_patch(group), patch_api.read_capabilities_from_patch(group). Remove inline function bodies from monolith. Verify get_fixture_model_input still offers manual fallback when patch returns nil (UX-01, D-114). Capability table shape unchanged for show_assessment hints (UX-02, D-115).
  </action>
  <verify>
    <automated>test -f lua/patch_api.lua && grep -q 'get_fixture_from_patch' lua/patch_api.lua && grep -q 'read_capabilities_from_patch' lua/patch_api.lua && ! grep -q 'local function get_fixture_from_patch' lua/SekonicCalibrator.lua && ! grep -q 'local function read_capabilities_from_patch' lua/SekonicCalibrator.lua && lua5.4 tests/run.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - patch_api.lua exists with both patch functions
    - load_domain_modules registers patch_api (D-108)
    - Monolith delegates; no duplicate patch function definitions
    - UX-01 auto make/model + manual fallback wiring preserved at call site
    - UX-02 capability flags available to assessment unchanged
  </acceptance_criteria>
  <done>patch_api layer extracted.</done>
</task>

<task type="auto">
  <name>Task 3: Extract fixture_apply.lua (CAL-04, D-106)</name>
  <files>lua/fixture_apply.lua, lua/SekonicCalibrator.lua</files>
  <read_first>lua/SekonicCalibrator.lua (select_group, apply_color_xyY, apply_color_hsb, calibrate_group L1316–1347), 06-CONTEXT.md CAL-04, ARCHITECTURE.md fixture_apply</read_first>
  <action>
    Create lua/fixture_apply.lua returning module M. Move select_group(group), apply_color_xyY(x,y), apply_color_hsb(x,y), calibrate_group(group,x,y) verbatim from monolith. calibrate_group must: Cmd select group; try SetColor("xyY", x, y, 1.0) first; on failure fall back to apply_color_hsb; return {success=bool, method="xyY"|"HSB"|nil}. Module may require color_math at top if needed for HSB conversion (apply_color_hsb uses existing xy→HSB logic — move unchanged). Extend load_domain_modules with fixture_apply = try_require("fixture_apply"). Replace all monolith calibrate_group/select_group calls with fixture_apply.*. Remove inline Section 4 bodies. Run lua5.4 tests/run.lua (D-120).
  </action>
  <verify>
    <automated>test -f lua/fixture_apply.lua && grep -q 'calibrate_group' lua/fixture_apply.lua && grep -q 'SetColor' lua/fixture_apply.lua && grep -q 'apply_color_hsb' lua/fixture_apply.lua && ! grep -q 'local function calibrate_group' lua/SekonicCalibrator.lua && lua5.4 tests/run.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - fixture_apply.lua exports all four apply functions
    - xyY primary + HSB fallback preserved with Y=1.0 (CAL-04)
    - load_domain_modules registers fixture_apply
    - Monolith has no inline calibrate_group definition
    - Host tests green (D-120)
  </acceptance_criteria>
  <done>Wave 2 complete — patch and apply layers extracted; shadowing fixed.</done>
</task>

</tasks>

<verification>
- goal_eval / session_goals rename complete; grep guard passes
- patch_api.lua and fixture_apply.lua extracted
- No duplicate function bodies in monolith for patch/apply
- lua5.4 tests/run.lua regression green
</verification>

<success_criteria>
- UX-01: Auto make/model from patch with manual fallback preserved
- UX-02: Feature hints from patch capabilities preserved
- CAL-04: SetColor xyY with HSB fallback unchanged
- D-106: Wave 2 modules extracted per architecture build order
</success_criteria>

<output>
Create `.planning/phases/06-operator-ux-docs-show-readiness/06-02-SUMMARY.md` when done
</output>
