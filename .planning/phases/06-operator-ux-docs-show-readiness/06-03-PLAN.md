---
phase: 06-operator-ux-docs-show-readiness
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - lua/ui/dialogs.lua
  - lua/ui/session.lua
  - lua/ui/measurement.lua
  - lua/ui/assessment.lua
  - lua/ui/history.lua
  - lua/calibration.lua
  - lua/bridge_client.lua
  - lua/SekonicCalibrator.lua
  - tests/test_session_format.lua
  - tests/run.lua
  - .planning/phases/06-operator-ux-docs-show-readiness/06-VALIDATION.md
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-05
  - CAL-06
  - DB-02
  - DB-03
  - UX-03
  - UX-04
must_haves:
  truths:
    - bridge_client.lua merged from Phase 5 product branch if absent; inline Section 2c removed (Wave 0)
    - ui/dialogs, ui/session, ui/measurement, ui/assessment, ui/history extracted with verbatim MessageBox strings (D-109)
    - calibration.lua owns outer/inner loops, auto-loop, history pre-fill wiring (CAL-03, D-110–D-113)
    - ui/measurement.lua delegates remote path to bridge_client.fetch_measurement — no inline HTTP (D-112)
    - Session goals once at start; target + reference modes preserved (CAL-01, CAL-02, D-110, D-111)
    - Manual Sekonic entry for C-700/C-800/C-7000 always available (CAL-06, D-112)
    - Session summary pass/fail per group (CAL-05, D-113)
    - History pre-fill and fixture history viewer preserved (DB-02, DB-03, D-118, D-119)
    - Gel hints and quality assessment bands unchanged (UX-03, UX-04, D-116, D-117)
    - SekonicCalibrator.lua thin shell ~150–250 lines; plugin.xml unchanged (D-107)
    - loadfile fallback for ui/* modules per D-23/D-108
    - lua5.4 tests/run.lua and pytest tests/test_bridge_routes.py green (D-120, D-121)
    - 06-VALIDATION.md nyquist_compliant true with task statuses updated
  artifacts:
    - lua/ui/dialogs.lua
    - lua/ui/session.lua
    - lua/ui/measurement.lua
    - lua/ui/assessment.lua
    - lua/ui/history.lua
    - lua/calibration.lua
    - lua/bridge_client.lua
  key_links:
    - from: ui/measurement.lua
      to: bridge_client.lua
      via: bridge_client.fetch_measurement(config)
      pattern: fetch_measurement|bridge_client
    - from: calibration.lua
      to: ui/assessment.lua
      via: show_assessment before apply
      pattern: show_assessment
    - from: SekonicCalibrator.lua
      to: calibration.lua
      via: calibration.run(display, deps)
      pattern: calibration%.run|require.*calibration
---

<objective>
Complete UI modularization and calibration orchestrator extraction; merge bridge_client if missing; sign off Phase 6 validation gates.

Purpose: Satisfies CAL-01–06, DB-02/03, UX-03/04; delivers thin entry shell per D-107–D-109.
Output: ui/* modules, calibration.lua, thin SekonicCalibrator.lua, bridge_client wired, full test suite green, validation doc signed off.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operator-ux-docs-show-readiness/06-CONTEXT.md
@.planning/phases/06-operator-ux-docs-show-readiness/06-RESEARCH.md
@.planning/phases/06-operator-ux-docs-show-readiness/06-VALIDATION.md
@.planning/phases/05-ma3-http-integration/05-01-PLAN.md
@.planning/research/ARCHITECTURE.md
@lua/SekonicCalibrator.lua
@lua/goals.lua
@lua/color_math.lua
@lua/fixture_db.lua
@tests/run.lua
</context>

<artifacts_this_phase_produces>
- `lua/bridge_client.lua` + `tests/test_bridge_client.lua` — cherry-pick/merge from Phase 5 product branch if workspace lacks them (Wave 0)
- `lua/ui/dialogs.lua` — get_number_input, show_result, shared MessageBox helpers
- `lua/ui/session.lua` — get_session_goals, get_spectral_goals, get_reference_measurements, goals_summary_line, show_session_summary
- `lua/ui/measurement.lua` — get_measurement_params (remote via bridge_client + manual fallback)
- `lua/ui/assessment.lua` — show_assessment with gel hints and QUALITY bands
- `lua/ui/history.lua` — show_fixture_history
- `lua/calibration.lua` — get_group_input, get_fixture_model_input, apply_historical_prefill, ask_group_done, ask_calibrate_another, outer/inner loops, auto-loop
- Thin `lua/SekonicCalibrator.lua` — load_domain_modules, main menu, Bridge Status, delegate to calibration.run
- Optional `tests/test_session_format.lua` for goals_summary_line pure format
- Updated `06-VALIDATION.md` sign-off
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Merge bridge_client + extract ui/dialogs and ui/session (CAL-01, CAL-02, CAL-05, Wave 0)</name>
  <files>lua/bridge_client.lua, tests/test_bridge_client.lua, lua/ui/dialogs.lua, lua/ui/session.lua, lua/SekonicCalibrator.lua, tests/test_session_format.lua, tests/run.lua</files>
  <read_first>06-RESEARCH.md (bridge_client absent note, Wave 0 gaps), 05-01-PLAN.md, lua/SekonicCalibrator.lua (Section 2c L839+, session L96–290, L521–736), git log / origin for bridge_client.lua</read_first>
  <action>
    **Wave 0 — bridge_client merge:** If lua/bridge_client.lua missing, cherry-pick or copy from Phase 5 product branch (claude/lighttune-main or cursor branch with Phase 5 complete) including tests/test_bridge_client.lua. Register bridge_client in load_domain_modules. Remove inline Section 2c (_http_request, bridge_fetch_measurement, bridge_check_status, show_bridge_status, run_bridge_setup) from monolith — delegate to bridge_client module and thin wrappers only if needed for Bridge Status menu until moved to ui. Add test_bridge_client to tests/run.lua if merged. **ui/dialogs.lua:** Extract get_number_input, show_result and any shared MessageBox helpers (L96–109, L666–678) — copy strings verbatim (D-109). Export table; load via try_require with loadfile fallback path `plugin_dir/lua/ui/dialogs.lua` (extend try_require or add try_require_ui helper per D-108). **ui/session.lua:** Extract get_one_spectral_goal, get_spectral_goals, get_reference_measurements, get_session_goals, goals_summary_line, show_session_summary (L111–290, L521–535, L695–736). Wire goal_eval module for spectral goal modes. Preserve MODE_TARGET/MODE_REFERENCE branching (CAL-02, D-111). Session goals set once at session start (CAL-01, D-110). Session summary pass/fail lines unchanged (CAL-05, D-113). Optional: extract goals_summary_line format to testable pure fn; add tests/test_session_format.lua. Update monolith to require ui modules; remove extracted inline bodies. Run lua5.4 tests/run.lua.
  </action>
  <verify>
    <automated>test -f lua/bridge_client.lua && test -f lua/ui/dialogs.lua && test -f lua/ui/session.lua && grep -q 'get_session_goals' lua/ui/session.lua && ! grep -q 'local function get_session_goals' lua/SekonicCalibrator.lua && lua5.4 tests/run.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - bridge_client.lua present; inline Section 2c HTTP removed from monolith
    - ui/dialogs.lua and ui/session.lua extracted with verbatim UI strings
    - CAL-01 session goals dialog preserved (meter, mode, cct, duv, spectral)
    - CAL-02 target vs reference mode preserved
    - CAL-05 show_session_summary preserved
    - tests/run.lua green including bridge_client tests if merged (D-120)
  </acceptance_criteria>
  <done>bridge_client merged; dialogs and session UI modules extracted.</done>
</task>

<task type="auto">
  <name>Task 2: Extract ui/measurement, ui/assessment, ui/history + calibration.lua (CAL-03, CAL-06, DB-02, DB-03, UX-03, UX-04, D-107)</name>
  <files>lua/ui/measurement.lua, lua/ui/assessment.lua, lua/ui/history.lua, lua/calibration.lua, lua/SekonicCalibrator.lua</files>
  <read_first>lua/SekonicCalibrator.lua (get_measurement_params L396–519, show_assessment L540–665, show_fixture_history L739–837, main loops L1488–1738, apply_historical_prefill L365–390), 06-CONTEXT.md D-112–D-119, patch_api.lua and fixture_apply.lua from Wave 2</read_first>
  <action>
    **ui/measurement.lua:** Move get_measurement_params verbatim. Remote path calls bridge_client.fetch_measurement(config) — NOT inline HTTP. Preserve Remote Measurement / Enter Manually / Cancel flow and C-700 vs C-7000 field sets (CAL-06, D-112). **ui/assessment.lua:** Move show_assessment verbatim — parameter session_goals (not goals); use goal_eval.goal_status_str; preserve gel hints via color_math.gel_hint and caps rules (UX-03, D-116); preserve QUALITY rating bands and Apply/Skip buttons (UX-04, D-117). **ui/history.lua:** Move show_fixture_history verbatim (DB-03, D-119). **calibration.lua:** Export calibration.run(display, deps) where deps bundles config_mod, patch_api, fixture_apply, fixture_db, color_math, goal_eval, bridge_client, ui modules. Move get_group_input, get_fixture_model_input (calls patch_api.get_fixture_from_patch), apply_historical_prefill (DB-02, D-118), ask_group_done, ask_calibrate_another, outer/inner calibration loops from main (CAL-03, D-110). Preserve MAX_AUTO_ATTEMPTS = 3, bridge_active, user_manual, auto-loop calling goal_eval.goals_met (D-113 partial). Wire find_best_for_fixture pre-fill before first measure (DB-02). Use fixture_apply.calibrate_group for apply step (CAL-04 via deps). Remove all moved function bodies from monolith. load_domain_modules adds calibration = try_require("calibration").
  </action>
  <verify>
    <automated>test -f lua/ui/measurement.lua && test -f lua/ui/assessment.lua && test -f lua/ui/history.lua && test -f lua/calibration.lua && grep -q 'MAX_AUTO_ATTEMPTS = 3' lua/calibration.lua && grep -q 'fetch_measurement' lua/ui/measurement.lua && grep -q 'show_fixture_history' lua/ui/history.lua && grep -q 'apply_historical_prefill' lua/calibration.lua && ! grep -q 'local function show_assessment' lua/SekonicCalibrator.lua && lua5.4 tests/run.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - All five ui modules and calibration.lua exist
    - CAL-03 inner loop measure→assess→apply preserved in calibration.lua
    - CAL-06 manual entry always available via ui/measurement.lua
    - DB-02 history pre-fill wired in calibration.run
    - DB-03 history viewer in ui/history.lua
    - UX-03 gel hints and UX-04 assessment screen strings unchanged
    - Monolith no longer contains show_assessment, get_measurement_params, main calibration loops
  </acceptance_criteria>
  <done>UI and orchestration layers extracted; v0.4 workflow preserved in modules.</done>
</task>

<task type="auto">
  <name>Task 3: Thin entry shell + Phase 6 validation sign-off (D-107, D-108, D-120, D-121)</name>
  <files>lua/SekonicCalibrator.lua, .planning/phases/06-operator-ux-docs-show-readiness/06-VALIDATION.md</files>
  <read_first>lua/SekonicCalibrator.lua (main L1453+), plugin.xml, 06-VALIDATION.md, 06-RESEARCH.md (shell size estimate), ARCHITECTURE.md thin shell pattern</read_first>
  <action>
    Refactor SekonicCalibrator.lua to thin shell (~150–250 lines per D-107): retain load_domain_modules (all modules including ui/* with loadfile fallback for nested paths), main(display) with pcall wrapper, config load, main menu (Start Calibration / View Fixture History / Bridge Status / Cancel), Bridge Status calling bridge_client.check_status + setup helpers (or ui bridge status if extracted), View History → ui.history.show_fixture_history, Start Calibration → calibration.run(display, deps). plugin.xml stays ComponentLua → lua/SekonicCalibrator.lua — do NOT rename entry unless require fails on console (D-107 discretion). Run full validation suite: `lua5.4 tests/run.lua` and `pytest tests/test_bridge_routes.py -v` (D-121 — bridge behavior unchanged). Run doc grep audit from Wave 1. Grep mechanical checks: no duplicate `local function get_session_goals` in shell; no `_http_request` in shell; `goal_eval` not shadowed. Update 06-VALIDATION.md: mark all task rows green, set nyquist_compliant true, wave_0_complete true, approval pending→ready. Line count check: `wc -l lua/SekonicCalibrator.lua` should be ≤300 (target 150–250).
  </action>
  <verify>
    <automated>wc -l lua/SekonicCalibrator.lua | awk '{if ($1<=300) exit 0; else exit 1}' && grep -q 'calibration' lua/SekonicCalibrator.lua && ! grep -q 'local function _http_request' lua/SekonicCalibrator.lua && ! grep -q 'local function get_session_goals' lua/SekonicCalibrator.lua && lua5.4 tests/run.lua && pytest tests/test_bridge_routes.py -v; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - SekonicCalibrator.lua is thin shell delegating to calibration.run
    - plugin.xml entry unchanged
    - Full host + bridge test suite green (D-120, D-121)
    - Doc grep audit still passes (TOP-01/UX-05)
    - 06-VALIDATION.md updated with nyquist_compliant true
    - All Phase 6 requirement IDs covered across plans 01–03
  </acceptance_criteria>
  <done>Wave 3 complete — Phase 6 modularization and show-readiness validation signed off.</done>
</task>

</tasks>

<verification>
- bridge_client merged; Section 2c removed from monolith
- ui/* + calibration.lua extracted; shell ≤300 lines
- MAX_AUTO_ATTEMPTS = 3 in calibration.lua
- goal_eval/session_goals naming consistent
- lua5.4 tests/run.lua + pytest green
- 06-VALIDATION.md nyquist_compliant true
- Doc grep: 127.0.0.1 in README, sekonic-bridge README, config example
</verification>

<success_criteria>
- CAL-01–06: Full v0.4 calibration workflow preserved through modular layers
- DB-02/03: History pre-fill and fixture history viewer preserved
- UX-03/04: Gel hints and quality assessment unchanged
- D-107–D-109: Thin shell, loader pattern, behavior preservation
- Phase 6 ready for Phase 7 macOS onPC UAT against published runbook
</success_criteria>

<output>
Create `.planning/phases/06-operator-ux-docs-show-readiness/06-03-SUMMARY.md` when done
</output>
