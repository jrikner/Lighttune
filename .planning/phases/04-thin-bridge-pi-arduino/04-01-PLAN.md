---
phase: 04-thin-bridge-pi-arduino
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sekonic-bridge/meter_backend.py
  - sekonic-bridge/meter_c7000_bulk.py
  - sekonic-bridge/meter_c7000_hid.py
  - sekonic-bridge/server.py
  - sekonic-bridge/discover_device.py
  - sekonic-bridge/setup-pi.sh
  - sekonic-bridge/build-image.sh
  - sekonic-bridge/start.sh
  - sekonic-bridge/README.md
autonomous: true
requirements:
  - MTR-02
must_haves:
  truths:
    - meter_c7000_bulk.py exists; meter_c7000_hid.py removed (D-66)
    - C7000Bulk class implements skreader bulk protocol; behavior unchanged from C7000HID
    - MeterBackend protocol documents connect/is_connected/disconnect/measure contract (D-67)
    - MockMeter and C7000Bulk both satisfy MeterBackend; server _load_meter uses bulk import
    - All packaging scripts reference meter_c7000_bulk.py not meter_c7000_hid.py (D-70)
    - pytest tests/test_bridge_routes.py still passes (no auth changes yet)
  artifacts:
    - sekonic-bridge/meter_backend.py
    - sekonic-bridge/meter_c7000_bulk.py
  key_links:
    - from: server.py
      to: meter_c7000_bulk.py
      via: from meter_c7000_bulk import C7000Bulk
      pattern: C7000Bulk|meter_c7000_bulk
    - from: meter_c7000_bulk.py
      to: meter_backend.py
      via: implements MeterBackend protocol
      pattern: MeterBackend
---

<objective>
Rename the misnamed HID driver to bulk naming and introduce a MeterBackend protocol seam without changing HTTP behavior or the in-plugin setup wizard contract.

Purpose: Satisfies MTR-02 (C-7000 USB bulk driver renamed/refactored from HID misnomer). Foundation for Wave 2 auth and Wave 3 internal setup thinning.
Output: meter_backend.py, meter_c7000_bulk.py; deleted meter_c7000_hid.py; updated imports in server and Pi packaging scripts.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-thin-bridge-pi-arduino/04-CONTEXT.md
@.planning/phases/04-thin-bridge-pi-arduino/04-RESEARCH.md
@.planning/codebase/CONCERNS.md
@sekonic-bridge/server.py
@sekonic-bridge/meter_c7000_hid.py
@sekonic-bridge/meter_mock.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MeterBackend protocol module (MTR-02, D-67)</name>
  <files>sekonic-bridge/meter_backend.py</files>
  <read_first>meter_c7000_hid.py, meter_mock.py, 04-CONTEXT.md</read_first>
  <action>
    Create sekonic-bridge/meter_backend.py with typing.Protocol (Python 3.12) defining MeterBackend: connect() -> bool, is_connected() -> bool, disconnect(), measure() -> dict. Document expected measure() keys (cct, duv, cri, r9, optional tlci). No runtime enforcement beyond typing — MockMeter and C7000Bulk duck-type satisfy it.
  </action>
  <verify>
    <automated>test -f sekonic-bridge/meter_backend.py && grep -q 'Protocol' sekonic-bridge/meter_backend.py && grep -q 'measure' sekonic-bridge/meter_backend.py</automated>
  </verify>
  <acceptance_criteria>
    - meter_backend.py exists with MeterBackend protocol
    - Documents measure() return shape matching /measure JSON contract
  </acceptance_criteria>
  <done>MeterBackend seam ready for mock and bulk drivers.</done>
</task>

<task type="auto">
  <name>Task 2: Rename meter_c7000_hid.py → meter_c7000_bulk.py (MTR-02, D-66)</name>
  <files>sekonic-bridge/meter_c7000_bulk.py, sekonic-bridge/meter_c7000_hid.py, sekonic-bridge/server.py, sekonic-bridge/discover_device.py</files>
  <read_first>meter_c7000_hid.py, server.py, discover_device.py, 04-CONTEXT.md</read_first>
  <action>
    Per D-66: git mv or copy meter_c7000_hid.py to meter_c7000_bulk.py. Rename class C7000HID to C7000Bulk. Update module docstring to emphasize USB bulk (skreader). Delete meter_c7000_hid.py. Update server.py _load_meter to import C7000Bulk from meter_c7000_bulk. Update discover_device.py references. Grep repo — zero remaining meter_c7000_hid or C7000HID references except CHANGELOG/planning if any.
  </action>
  <verify>
    <automated>test -f sekonic-bridge/meter_c7000_bulk.py && ! test -f sekonic-bridge/meter_c7000_hid.py && grep -q 'C7000Bulk' sekonic-bridge/server.py && ! grep -rq 'meter_c7000_hid' sekonic-bridge/ --include='*.py' --include='*.sh'</automated>
  </verify>
  <acceptance_criteria>
    - meter_c7000_bulk.py exists; meter_c7000_hid.py removed
    - server.py loads C7000Bulk for hardware mode
    - discover_device.py updated
  </acceptance_criteria>
  <done>HID misnomer eliminated; bulk naming matches implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Update Pi packaging scripts and README (D-69, D-70)</name>
  <files>sekonic-bridge/setup-pi.sh, sekonic-bridge/build-image.sh, sekonic-bridge/start.sh, sekonic-bridge/README.md</files>
  <read_first>setup-pi.sh, build-image.sh, sekonic-bridge/README.md, 04-CONTEXT.md</read_first>
  <action>
    Update file copy lists and references from meter_c7000_hid.py to meter_c7000_bulk.py in setup-pi.sh, build-image.sh, start.sh. README: replace HID filename references with bulk naming; note module implements USB bulk per skreader. Do NOT remove /capture or /learn_trigger docs (D-57). Run pytest tests/test_bridge_routes.py — must pass. Commit to claude/lighttune-main per D-74.
  </action>
  <verify>
    <automated>grep -q 'meter_c7000_bulk.py' sekonic-bridge/setup-pi.sh && grep -q 'meter_c7000_bulk.py' sekonic-bridge/build-image.sh && pytest tests/test_bridge_routes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Packaging scripts copy meter_c7000_bulk.py
    - README reflects bulk naming
    - pytest 3 passed
  </acceptance_criteria>
  <done>Wave 1 complete — MTR-02 driver rename with zero HTTP contract change.</done>
</task>

</tasks>

<verification>
- meter_c7000_bulk.py exists; meter_c7000_hid.py gone
- MeterBackend protocol module present
- pytest green
- Setup routes unchanged (manual spot-check /status fields)
</verification>

<success_criteria>
- MTR-02: Bulk driver renamed and refactored from HID misnomer
- No regression in bridge route tests or mock measure behavior
</success_criteria>

<output>
Create `.planning/phases/04-thin-bridge-pi-arduino/04-01-SUMMARY.md` when done
</output>

## PLANNING COMPLETE
