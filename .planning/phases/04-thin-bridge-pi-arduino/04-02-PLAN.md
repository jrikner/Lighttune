---
phase: 04-thin-bridge-pi-arduino
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - sekonic-bridge/server.py
  - sekonic-bridge/bridge_config.json.example
  - sekonic-bridge/README.md
  - lua/SekonicCalibrator.lua
  - data/config.json.example
  - tests/conftest.py
  - tests/test_bridge_routes.py
  - tests/test_bridge_auth.py
  - tests/fixtures/bridge_status_ok.json
autonomous: true
requirements:
  - MTR-08
must_haves:
  truths:
    - Bridge reads API key from BRIDGE_API_KEY env or bridge_config.json bridge_api_key (D-62)
    - When key unset, all routes behave as today (open LAN) (D-63)
    - When key set, missing/wrong X-Bridge-Key returns 401 on /status, /measure, /discover, /capture, /learn_trigger (D-64)
    - /status includes auth_required boolean alongside existing setup flags (D-58, D-59)
    - Plugin load_config parses bridge_api_key; _http_request sends X-Bridge-Key on every call when set (D-65)
    - pytest covers 401 without key and 200 with key on core routes
  artifacts:
    - sekonic-bridge/bridge_config.json.example
    - tests/test_bridge_auth.py
  key_links:
    - from: lua/SekonicCalibrator.lua
      to: sekonic-bridge/server.py
      via: X-Bridge-Key header on raw HTTP/1.0 request
      pattern: X-Bridge-Key|bridge_api_key
    - from: tests/test_bridge_auth.py
      to: server.py
      via: TestClient with BRIDGE_API_KEY env
      pattern: 401|X-Bridge-Key
---

<objective>
Implement optional shared-secret auth on the bridge and plugin so show LAN exposure can be gated without breaking the in-plugin setup wizard.

Purpose: Satisfies MTR-08 and ROADMAP Phase 4 success criterion #3. Setup routes must accept authenticated requests when key is configured (D-64, D-75).
Output: bridge_config.json.example, auth middleware/dependency, plugin header support, pytest auth suite, updated golden fixture.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-thin-bridge-pi-arduino/04-CONTEXT.md
@.planning/phases/04-thin-bridge-pi-arduino/04-RESEARCH.md
@sekonic-bridge/server.py
@lua/SekonicCalibrator.lua
@tests/test_bridge_routes.py
@tests/conftest.py
@tests/fixtures/bridge_status_ok.json
@data/config.json.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bridge API key loading and enforcement (MTR-08, D-61–D-64)</name>
  <files>sekonic-bridge/server.py, sekonic-bridge/bridge_config.json.example, sekonic-bridge/README.md</files>
  <read_first>server.py, 04-CONTEXT.md, INTEGRATIONS.md</read_first>
  <action>
    Add _load_bridge_api_key() reading BRIDGE_API_KEY env first, then bridge_config.json field bridge_api_key. Add FastAPI dependency or middleware verifying X-Bridge-Key header when key is non-empty; return 401 JSON on mismatch. Apply to ALL routes including /capture and /learn_trigger. Extend GET /status response with auth_required boolean (true when key configured). Update bridge_config.json.example. Document in sekonic-bridge/README.md: optional auth, header name, env var, example config. When key unset, behavior identical to pre-Phase-4.
  </action>
  <verify>
    <automated>test -f sekonic-bridge/bridge_config.json.example && grep -q 'auth_required' sekonic-bridge/server.py && grep -q 'X-Bridge-Key' sekonic-bridge/README.md</automated>
  </verify>
  <acceptance_criteria>
    - Key load from env + bridge_config.json
    - 401 when key set and header missing/wrong
    - auth_required on /status
    - Open LAN when key unset
  </acceptance_criteria>
  <done>Bridge-side MTR-08 enforcement complete.</done>
</task>

<task type="auto">
  <name>Task 2: Plugin config and HTTP header (MTR-08, D-65)</name>
  <files>lua/SekonicCalibrator.lua, data/config.json.example</files>
  <read_first>lua/SekonicCalibrator.lua (_http_request, load_config), 04-CONTEXT.md</read_first>
  <action>
    Extend load_config() to parse optional bridge_api_key from config.json (regex match like bridge_ip). Modify _http_request() to insert X-Bridge-Key header line when config.bridge_api_key is non-empty — before the blank line terminating HTTP/1.0 headers. Update data/config.json.example with commented bridge_api_key field. Do NOT change bridge_fetch_measurement parse logic or setup wizard flow structure.
  </action>
  <verify>
    <automated>grep -q 'bridge_api_key' lua/SekonicCalibrator.lua && grep -q 'X-Bridge-Key' lua/SekonicCalibrator.lua && grep -q 'bridge_api_key' data/config.json.example</automated>
  </verify>
  <acceptance_criteria>
    - load_config returns bridge_api_key when present
    - _http_request sends X-Bridge-Key when configured
    - config.json.example documents field
  </acceptance_criteria>
  <done>Plugin sends shared secret on setup and measure calls.</done>
</task>

<task type="auto">
  <name>Task 3: Pytest auth suite and golden fixture update (D-72)</name>
  <files>tests/conftest.py, tests/test_bridge_auth.py, tests/test_bridge_routes.py, tests/fixtures/bridge_status_ok.json</files>
  <read_first>tests/conftest.py, tests/test_bridge_routes.py, 04-CONTEXT.md, 03-CONTEXT.md D-47</read_first>
  <action>
    Add tests/test_bridge_auth.py: fixture enabling BRIDGE_API_KEY=test-secret; test /status returns 401 without header, 200 with X-Bridge-Key; same for POST /measure and GET /discover. Update bridge_status_ok.json golden to include setup flags AND auth_required:false. Ensure existing test_bridge_routes.py still passes with no key configured (monkeypatch unset env). CI job unchanged — auth tests run in same pytest invocation. Commit to claude/lighttune-main.
  </action>
  <verify>
    <automated>pytest tests/test_bridge_routes.py tests/test_bridge_auth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_bridge_auth.py exists with 401/200 cases
    - Golden fixture updated with auth_required
    - All pytest passes locally
    - Phase 3 CI trio still green without key in workflow env
  </acceptance_criteria>
  <done>MTR-08 verified by automated tests; setup routes auth-covered.</done>
</task>

</tasks>

<verification>
- pytest tests/test_bridge_routes.py tests/test_bridge_auth.py green
- Manual: mock server with BRIDGE_API_KEY + plugin config bridge_api_key (document in summary)
- Setup flags still on /status
</verification>

<success_criteria>
- MTR-08: Bridge rejects unauthenticated requests when bridge_api_key configured
- Plugin sends matching header from config.json
- In-plugin wizard paths remain callable with auth enabled
</success_criteria>

<output>
Create `.planning/phases/04-thin-bridge-pi-arduino/04-02-SUMMARY.md` when done
</output>

## PLANNING COMPLETE
