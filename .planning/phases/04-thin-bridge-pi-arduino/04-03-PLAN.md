---
phase: 04-thin-bridge-pi-arduino
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - sekonic-bridge/server.py
  - sekonic-bridge/README.md
  - tests/test_bridge_setup_routes.py
  - .planning/phases/04-thin-bridge-pi-arduino/04-VALIDATION.md
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - MTR-01
  - MTR-07
must_haves:
  truths:
    - sekonic-bridge contains no color math, goals, or calibration logic — transport + setup only (MTR-01)
    - C-7000 (VID 0x0A41) setup routes use bulk fast-path; HID probe grid skipped when bulk active (D-56)
    - /capture, /learn_trigger, /discover JSON shapes unchanged for plugin regex parsers (D-55, D-75)
    - Mock mode (--mock) returns realistic improving readings; setup routes succeed in mock (MTR-07, D-60)
    - Optional pytest smoke for /capture mock mode — not required for CI green (D-72, Phase 3 D-47)
    - 04-VALIDATION.md sign-off for MTR-01, MTR-02, MTR-07, MTR-08
  artifacts:
    - .planning/phases/04-thin-bridge-pi-arduino/04-VALIDATION.md
    - tests/test_bridge_setup_routes.py
  key_links:
    - from: run_bridge_setup (plugin)
      to: /discover, /capture, /learn_trigger
      via: unchanged HTTP contract after internal refactor
      pattern: run_bridge_setup|/capture
    - from: server.py
      to: meter_c7000_bulk.py
      via: bulk measure in /capture C-7000 path
      pattern: C7000Bulk|0x0A41
---

<objective>
Internally thin setup implementation for C-7000 while preserving the in-plugin wizard contract, then sign off Phase 4 with validation gates.

Purpose: Satisfies MTR-01 (raw meter fields only on Pi), MTR-07 (mock mode), and D-56 internal thinning. Confirms D-75 base-version setup still works.
Output: Optimized setup paths in server.py; optional setup route tests; 04-VALIDATION.md; CI unchanged for required gates.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-thin-bridge-pi-arduino/04-CONTEXT.md
@.planning/phases/04-thin-bridge-pi-arduino/04-RESEARCH.md
@.planning/ROADMAP.md
@sekonic-bridge/server.py
@sekonic-bridge/meter_mock.py
@lua/SekonicCalibrator.lua
@tests/test_bridge_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: C-7000 bulk fast-path in setup routes (D-56, MTR-01)</name>
  <files>sekonic-bridge/server.py</files>
  <read_first>server.py (/capture, /learn_trigger, /discover), meter_c7000_bulk.py, 04-CONTEXT.md, CONCERNS.md</read_first>
  <action>
    Per D-56: For VID 0x0A41 (Sekonic C-7000), ensure /discover auto-sets protocol_captured and trigger_discovered in device_config.json (existing behavior preserved). /capture: prefer C7000Bulk test measure path; skip HID passive-listen fallback when bulk driver active. /learn_trigger: if bulk protocol captured and trigger_discovered already true, return success immediately without running _build_trigger_candidates probe loop. Keep HTTP response JSON keys identical to today (plugin regex depends on them). For non-C-7000 VID, retain existing fallback paths. Grep sekonic-bridge/ — no imports of color math or goals modules.
  </action>
  <verify>
    <automated>! grep -rqE 'color_math|goals\.|SetColor' sekonic-bridge/ --include='*.py' && grep -q '0x0A41' sekonic-bridge/server.py</automated>
  </verify>
  <acceptance_criteria>
    - No calibration logic in sekonic-bridge Python
    - C-7000 setup uses bulk fast-path
    - Response JSON shapes unchanged
  </acceptance_criteria>
  <done>Internal setup thinning complete without breaking plugin contract.</done>
</task>

<task type="auto">
  <name>Task 2: Optional setup route mock smoke tests (D-72, MTR-07)</name>
  <files>tests/test_bridge_setup_routes.py, tests/conftest.py</files>
  <read_first>server.py mock handlers for /capture, tests/test_bridge_routes.py, 03-CONTEXT.md D-47</read_first>
  <action>
    Add tests/test_bridge_setup_routes.py (optional, not CI gate): with --mock client, POST /capture returns 200 and body indicates protocol_captured; GET /discover returns configured true. Document in test module header that these are smoke tests excluded from minimal CI if desired — but include in default pytest run. Verify MockMeter measure progression still returns cct/duv/cri/r9 on POST /measure. Do NOT add /capture or /learn_trigger to required CI workflow unless all pass reliably under 5s timeout.
  </action>
  <verify>
    <automated>test -f tests/test_bridge_setup_routes.py && pytest tests/test_bridge_setup_routes.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Setup smoke tests exist and pass in mock mode
    - /measure mock progression unchanged
    - CI workflow still gates only core trio if setup tests flaky
  </acceptance_criteria>
  <done>MTR-07 mock setup path verified.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 4 validation gate (MTR-01–08, D-75)</name>
  <files>.planning/phases/04-thin-bridge-pi-arduino/04-VALIDATION.md, sekonic-bridge/README.md</files>
  <read_first>02-VALIDATION.md, 03-VALIDATION.md, 04-CONTEXT.md, ROADMAP.md Phase 4</read_first>
  <action>
    Create 04-VALIDATION.md with per-plan task map and sign-off gates: (1) meter_c7000_bulk.py + no hid file (MTR-02), (2) pytest core + auth green (MTR-08), (3) lua5.4 tests/run.lua 139+ PASS unchanged, (4) no cal logic in bridge (MTR-01), (5) mock measure works (MTR-07), (6) manual checklist: plugin Bridge Status + Setup wizard steps (discover→capture→status flags) — D-75 human verification item. Update sekonic-bridge/README thin-bridge summary: transport-only, setup via plugin wizard, optional API key. Run local verification commands and record results. Mark requirements MTR-01, MTR-02, MTR-07, MTR-08 in validation doc.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-thin-bridge-pi-arduino/04-VALIDATION.md && grep -q 'MTR-08' .planning/phases/04-thin-bridge-pi-arduino/04-VALIDATION.md && pytest tests/test_bridge_routes.py tests/test_bridge_auth.py -q && lua5.4 tests/run.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - 04-VALIDATION.md exists with all four MTR requirements
    - D-75 manual wizard checklist documented
    - Local pytest + lua suites green at validation time
  </acceptance_criteria>
  <done>Phase 4 ready for /gsd-execute-phase 4.</done>
</task>

</tasks>

<verification>
- Full pytest suite green
- lua5.4 tests/run.lua 139+ PASS
- Plugin wizard manual checklist in VALIDATION.md
- CI .github/workflows/ci.yml unchanged or explicitly still trio-only
</verification>

<success_criteria>
- MTR-01: Thin bridge — raw JSON transport, no calibration on Pi
- MTR-07: Mock mode realistic readings + setup smoke
- D-75: In-plugin setup preserved for base version
- Phase 4 ROADMAP success criteria 1–4 addressable post-execution
</success_criteria>

<output>
Create `.planning/phases/04-thin-bridge-pi-arduino/04-03-SUMMARY.md` when done
</output>

## PLANNING COMPLETE
