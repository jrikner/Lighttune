---
phase: 05-ma3-http-integration
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - lua/SekonicCalibrator.lua
  - tests/test_bridge_client.lua
  - tests/run.lua
  - .planning/phases/05-ma3-http-integration/05-VALIDATION.md
autonomous: true
requirements:
  - MTR-05
  - ARCH-04
must_haves:
  truths:
    - Auto-loop uses bridge_client.fetch_measurement for attempts 2+ (D-86–D-90)
    - MAX_AUTO_ATTEMPTS remains 3; stuck-after-3 dialog unchanged
    - Per-cycle Accept/Enter Manually/Cancel on auto-measurements preserved (D-88)
    - First measure Remote/Enter Manually/Cancel + Accept/Re-measure confirmation unchanged (D-87)
    - Stuck-after-3 dialog Accept & Move On / Try Again / Skip Group unchanged; Try Again resets loop_count only (D-89)
    - tests/test_bridge_client.lua exists with 15+ assertions (D-99)
    - tests/run.lua includes test_bridge_client runner (D-100)
    - lua5.4 tests/run.lua 154+ PASS; pytest 9 passed
    - 05-VALIDATION.md sign-off updated with execution results
  artifacts:
    - tests/test_bridge_client.lua
  key_links:
    - from: tests/test_bridge_client.lua
      to: lua/bridge_client.lua
      via: require bridge_client parse helpers
      pattern: parse_measure_body|classify_http
    - from: auto-loop inner repeat
      to: bridge_client.fetch_measurement
      via: attempt > 1 path
      pattern: MAX_AUTO_ATTEMPTS|fetch_measurement
---

<objective>
Verify auto-loop contract against bridge_client, add Lua host tests for parse/error mapping, and sign off Phase 5 validation gates.

Purpose: Satisfies MTR-05 (auto-loop with goals_met), D-99–D-101 host testing, phase validation sign-off.
Output: test_bridge_client.lua, updated run.lua, validation doc, auto-loop verified unchanged in behavior.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ma3-http-integration/05-CONTEXT.md
@.planning/phases/05-ma3-http-integration/05-VALIDATION.md
@lua/SekonicCalibrator.lua
@lua/bridge_client.lua
@tests/lib_assert.lua
@tests/fixtures/bridge_measure_ok.json
@tests/fixtures/bridge_status_ok.json
</context>

<artifacts_this_phase_produces>
- `tests/test_bridge_client.lua` — test runner function(M) with 15+ assertions
- Test cases: parse_measure_body happy path, missing cct, out-of-range cct, parse_status_body with auth_required, classify_http 401/503/504, unauthorized hint text path
- Updated `tests/run.lua` — require and run test_bridge_client
- Updated `05-VALIDATION.md` — task statuses and sign-off gates marked pass
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify auto-loop uses bridge_client (MTR-05, D-86–D-90)</name>
  <files>lua/SekonicCalibrator.lua</files>
  <read_first>lua/SekonicCalibrator.lua (auto-loop ~1544–1700), lua/goals.lua (goals_met), 05-CONTEXT.md</read_first>
  <action>
    Audit inner calibration loop: confirm MAX_AUTO_ATTEMPTS = 3 unchanged; attempt > 1 path calls bridge_fetch_measurement (which delegates to bridge_client); user_manual flag behavior preserved; stuck-after-3 MessageBox buttons Accept & Move On / Try Again / Skip Group unchanged; Accept/Enter Manually/Cancel on auto-measurement confirmation unchanged. Fix any remaining direct HTTP bypass. Add brief comment above MAX_AUTO_ATTEMPTS documenting MTR-05 contract if missing. No silent auto-accept changes (D-88).
  </action>
  <verify>
    <automated>grep -q 'MAX_AUTO_ATTEMPTS = 3' lua/SekonicCalibrator.lua && grep -q 'goals.goals_met' lua/SekonicCalibrator.lua && grep -q 'Stuck after' lua/SekonicCalibrator.lua</automated>
  </verify>
  <acceptance_criteria>
    - MAX_AUTO_ATTEMPTS equals 3
    - goals_met invoked in auto-loop success path
    - Stuck dialog with three buttons present
    - No new auto-accept shortcut introduced
  </acceptance_criteria>
  <done>Auto-loop contract verified against CONTEXT decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests/test_bridge_client.lua (D-99–D-100)</name>
  <files>tests/test_bridge_client.lua, tests/run.lua</files>
  <read_first>tests/test_goals.lua, tests/lib_assert.lua, lua/bridge_client.lua, tests/fixtures/bridge_measure_ok.json, tests/fixtures/bridge_status_ok.json</read_first>
  <action>
    Create tests/test_bridge_client.lua returning function(M). Load bridge_client via package.path ./lua/?.lua. Test parse_measure_body with fixture JSON string matching bridge_measure_ok shape; test missing field returns nil/false; test classify_http(401, body) kind unauthorized; test classify_http(503,...), classify_http(504,...); test parse_status_body includes auth_required false/true; test validation rejection for cct out of range. Minimum 15 assert calls. Update tests/run.lua to require and invoke test_bridge_client after test_goals. Do not require live socket in tests — test parse/classify helpers only.
  </action>
  <verify>
    <automated>test -f tests/test_bridge_client.lua && lua5.4 tests/run.lua 2>&1 | grep -E 'Results: [0-9]+ passed, 0 failed'</automated>
  </verify>
  <acceptance_criteria>
    - test_bridge_client.lua exists
    - tests/run.lua invokes it
    - Total PASS count >= 154 (139 baseline + 15 new)
    - 0 FAIL
  </acceptance_criteria>
  <done>Host tests cover bridge_client parse/error layer.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 5 validation sign-off (ARCH-04–MTR-06)</name>
  <files>.planning/phases/05-ma3-http-integration/05-VALIDATION.md</files>
  <read_first>05-VALIDATION.md, 03-VALIDATION.md (sign-off format), 05-CONTEXT.md</read_first>
  <action>
    Run lua5.4 tests/run.lua and pytest tests/test_bridge_routes.py tests/test_bridge_auth.py tests/test_bridge_setup_routes.py -q. Update 05-VALIDATION.md per-task map statuses to pass with recorded PASS counts; mark sign-off gates 1–7 with checkmarks and execution date. Document manual-only items (MA3 UAT) still pending Phase 7. Commit product changes to claude/lighttune-main; planning validation doc on planning branch per project convention.
  </action>
  <verify>
    <automated>grep -q 'MTR-05' .planning/phases/05-ma3-http-integration/05-VALIDATION.md && lua5.4 tests/run.lua; test $? -eq 0 && pytest tests/test_bridge_routes.py tests/test_bridge_auth.py tests/test_bridge_setup_routes.py -q; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - 05-VALIDATION.md updated with pass results
    - All automated gates green at validation time
    - D-75 manual wizard noted as Phase 7/verify-work follow-up
  </acceptance_criteria>
  <done>Phase 5 ready for /gsd-execute-phase 5.</done>
</task>

</tasks>

<verification>
- 154+ lua tests PASS
- pytest 9 passed
- Auto-loop contract unchanged
- VALIDATION.md signed off
</verification>

<success_criteria>
- MTR-05: Auto-loop verified with bridge_client and 3-attempt cap
- D-99/D-100: bridge_client host tests in CI path via tests/run.lua
- Phase 5 validation gates documented
</success_criteria>

<output>
Create `.planning/phases/05-ma3-http-integration/05-03-SUMMARY.md` when done
</output>

## PLANNING COMPLETE
