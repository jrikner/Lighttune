---
phase: 05-ma3-http-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - lua/SekonicCalibrator.lua
autonomous: true
requirements:
  - MTR-03
  - MTR-04
  - MTR-06
must_haves:
  truths:
    - bridge_fetch_measurement and bridge_check_status delegate to bridge_client (no duplicate HTTP logic)
    - format_bridge_error maps kind/http_status to operator-facing strings (D-83)
    - Bridge failure dialogs still offer Retry Remote, Enter Manually, Cancel (D-82)
    - Remote/Manual choice shown only when bridge_ip set AND goals.meter is C-7000 (D-91)
    - First remote measure keeps Remote/Enter Manually/Cancel then Accept/Re-measure/Enter Manually flow (D-87)
    - No mandatory /status pre-flight before every remote measure (D-93)
    - show_bridge_status displays auth_required and last_error when reachable (D-96)
    - run_bridge_setup and _run_trigger_discovery use bridge_client route helpers (D-95, D-98)
    - pytest bridge suite still passes (regression)
  artifacts:
    - lua/SekonicCalibrator.lua
  key_links:
    - from: get_measurement_params
      to: bridge_client.fetch_measurement
      via: structured result + format_bridge_error on failure
      pattern: fetch_measurement|format_bridge_error
    - from: show_bridge_status
      to: bridge_client.check_status
      via: auth_required, last_error in status message
      pattern: auth_required|last_error
---

<objective>
Wire the monolith to bridge_client, enrich remote failure UX, gate remote offer on C-7000 session meter, and enhance Bridge Status — without changing setup wizard steps or Pi routes.

Purpose: Satisfies MTR-03 (end-to-end remote measure), MTR-04 (failure UX), MTR-06 (status + setup menu).
Output: Refactored Section 2c call sites; C7000 remote gate; richer error text; enhanced Bridge Status display.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ma3-http-integration/05-CONTEXT.md
@.planning/phases/04-thin-bridge-pi-arduino/04-CONTEXT.md
@lua/SekonicCalibrator.lua
@lua/bridge_client.lua
@tests/test_bridge_routes.py
</context>

<artifacts_this_phase_produces>
- `format_bridge_error(result)` — local function in SekonicCalibrator.lua mapping bridge_client error kinds to operator strings
- Updated `bridge_fetch_measurement(config)` — returns measured table or nil, err_string via bridge_client
- Updated `bridge_check_status(config)` — returns reachable, connected, meter, flags, auth_required, last_error
- Updated `get_measurement_params` — C-7000 gate for remote offer
- Updated `show_bridge_status` — auth/error lines in MessageBox
- Updated `run_bridge_setup`, `_run_trigger_discovery` — use bridge_client discover/capture/learn_trigger
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire measure/status/setup to bridge_client (MTR-03, D-78–D-79)</name>
  <files>lua/SekonicCalibrator.lua</files>
  <read_first>lua/bridge_client.lua, lua/SekonicCalibrator.lua (Section 2c), 05-CONTEXT.md</read_first>
  <action>
    Replace bridge_fetch_measurement body to call bridge_client.fetch_measurement(config); on ok return data table; on failure return nil, format_bridge_error(result). Replace bridge_check_status to call bridge_client.check_status; map data fields to return values including auth_required and last_error booleans/strings. Update run_bridge_setup and _run_trigger_discovery to use bridge_client.discover, capture, learn_trigger — preserve existing MessageBox flow and regex checks on response data (success flags, trigger_cmd_hex). Remove duplicate _http_request implementation if still inline; all HTTP via bridge_client.
  </action>
  <verify>
    <automated>grep -q 'bridge_client.fetch_measurement' lua/SekonicCalibrator.lua && grep -q 'bridge_client.check_status' lua/SekonicCalibrator.lua && ! grep -q 'socket%.tcp' lua/SekonicCalibrator.lua</automated>
  </verify>
  <acceptance_criteria>
    - No raw socket code remains in SekonicCalibrator.lua
    - bridge_fetch_measurement and bridge_check_status delegate to module
    - Setup wizard HTTP calls use bridge_client helpers
  </acceptance_criteria>
  <done>Monolith HTTP fully delegated to bridge_client.</done>
</task>

<task type="auto">
  <name>Task 2: Error formatter + failure UX (MTR-04, D-82–D-85)</name>
  <files>lua/SekonicCalibrator.lua</files>
  <read_first>lua/SekonicCalibrator.lua (get_measurement_params, auto-loop error dialogs), 05-CONTEXT.md, sekonic-bridge/server.py HTTPException detail shapes</read_first>
  <action>
    Add local function format_bridge_error(err_result) using err_result.kind, message, hint, http_status. Map unauthorized→"Check bridge_api_key in config.json matches Pi BRIDGE_API_KEY"; connection→"Cannot reach bridge at {ip}"; timeout→"Meter did not respond in time"; http 503→"Meter not connected — check USB"; http 409→"Measurement already in progress". Use in Bridge Error MessageBox in get_measurement_params and auto-loop error path — preserve buttons Retry Remote, Enter Manually, Cancel (D-82). Preserve goto bridge_retry and attempt-1 retry semantics (D-84). Manual fallback paths unchanged (D-85).
  </action>
  <verify>
    <automated>grep -q 'format_bridge_error' lua/SekonicCalibrator.lua && grep -q 'Retry Remote' lua/SekonicCalibrator.lua && grep -q 'Enter Manually' lua/SekonicCalibrator.lua</automated>
  </verify>
  <acceptance_criteria>
    - format_bridge_error exists and references error kinds
    - All three failure buttons preserved in measure and auto-loop error dialogs
    - Error strings mention auth hint for unauthorized kind
  </acceptance_criteria>
  <done>MTR-04 enriched errors without UX regression.</done>
</task>

<task type="auto">
  <name>Task 3: C-7000 remote gate + Bridge Status enhancement (MTR-03, MTR-06, D-91–D-97)</name>
  <files>lua/SekonicCalibrator.lua</files>
  <read_first>lua/SekonicCalibrator.lua (get_measurement_params, show_bridge_status, main menu), 05-CONTEXT.md, Phase 4 D-75</read_first>
  <action>
    In get_measurement_params: wrap remote-offer block with condition config.bridge_ip non-empty AND goals.meter == METER_C7000 (use METER_C7000 constant). When bridge_ip set but meter is C-700/C-800, skip remote dialog; optional note in first manual prompt per D-92. In show_bridge_status: extend msg format string to include Auth line (Required/Not required or key missing warning when auth_required true and no bridge_api_key) and Last error line when last_error non-null. Keep main menu Bridge Status button unchanged (D-94). Do not add mid-calibration configure interrupt (D-97). Run pytest tests/test_bridge_routes.py tests/test_bridge_auth.py tests/test_bridge_setup_routes.py -q and lua5.4 tests/run.lua.
  </action>
  <verify>
    <automated>grep -q 'METER_C7000' lua/SekonicCalibrator.lua && grep -q 'auth_required' lua/SekonicCalibrator.lua && pytest tests/test_bridge_routes.py tests/test_bridge_auth.py tests/test_bridge_setup_routes.py -q && lua5.4 tests/run.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Remote choice gated on C-7000 session meter
    - Bridge Status shows auth_required and last_error
    - pytest 9 passed; lua regression green
  </acceptance_criteria>
  <done>Wave 2 complete — remote gate + status UX + wired client.</done>
</task>

</tasks>

<verification>
- Monolith uses bridge_client exclusively for HTTP
- C7000 remote gate active
- Bridge Status enhanced
- pytest + lua regression green
</verification>

<success_criteria>
- MTR-03: Remote measure path uses extracted client with parse contract
- MTR-04: Retry/Manual/Cancel with enriched errors
- MTR-06: Bridge Status + setup wizard preserved and enhanced
</success_criteria>

<output>
Create `.planning/phases/05-ma3-http-integration/05-02-SUMMARY.md` when done
</output>

## PLANNING COMPLETE
