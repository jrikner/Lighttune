---
phase: 05-ma3-http-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lua/bridge_client.lua
  - lua/SekonicCalibrator.lua
autonomous: true
requirements:
  - ARCH-04
  - MTR-03
must_haves:
  truths:
    - lua/bridge_client.lua exists and returns structured {ok, data} or {ok=false, kind, message}
    - Module exports fetch_measurement, check_status, discover, capture, learn_trigger (D-78)
    - Timeouts match D-81 (5/12/35/38/120 seconds per route)
    - X-Bridge-Key header sent when config.bridge_api_key non-empty (D-65 carry-forward)
    - load_domain_modules loads bridge_client alongside color_math, fixture_db, goals (D-77)
    - Section 2c _http_request logic removed from monolith or thin-delegates to bridge_client.request
  artifacts:
    - lua/bridge_client.lua
  key_links:
    - from: SekonicCalibrator.lua
      to: bridge_client.lua
      via: domain.bridge_client or local bridge_client = domain.bridge_client
      pattern: bridge_client
    - from: bridge_client.lua
      to: require("socket")
      via: TCP HTTP/1.0 transport
      pattern: socket%.tcp
---

<objective>
Extract the GrandMA3 HTTP bridge client into `lua/bridge_client.lua` with structured results and locked timeouts, and register the module in the domain loader.

Purpose: Satisfies ARCH-04 (bridge_client extract) and MTR-03 foundation (HTTP/1.0 + MeasurementRecord parse contract).
Output: New bridge_client module; monolith loader updated; Section 2c transport moved out of monolith (call sites may still be wired in Plan 02).
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ma3-http-integration/05-CONTEXT.md
@.planning/phases/05-ma3-http-integration/05-RESEARCH.md
@.planning/phases/02-plugin-hardening-test-seams/02-CONTEXT.md
@lua/SekonicCalibrator.lua
@.planning/codebase/INTEGRATIONS.md
</context>

<artifacts_this_phase_produces>
- `lua/bridge_client.lua` — module table with request, fetch_measurement, check_status, discover, capture, learn_trigger
- `bridge_client.parse_measure_body(body)` — regex parse helper (testable)
- `bridge_client.parse_status_body(body)` — regex parse helper (testable)
- `bridge_client.classify_http(status, body)` — maps to kind/message/hint
- Constants: CCT_MIN/CCT_MAX/DUV_MIN/DUV_MAX aligned with color_math or duplicated bounds for validation kind
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create bridge_client.lua transport + structured results (ARCH-04, D-77–D-80)</name>
  <files>lua/bridge_client.lua</files>
  <read_first>lua/SekonicCalibrator.lua (_http_request, bridge_fetch_measurement, bridge_check_status), 05-CONTEXT.md, 05-RESEARCH.md</read_first>
  <action>
    Create lua/bridge_client.lua returning a module table. Move _http_request logic here as `request(method, host, port, path, opts)` where opts includes timeout_s and api_key; build HTTP/1.0 request with optional X-Bridge-Key header line before blank line (same as Phase 4). On TCP failure return {ok=false, kind="connection", message=...}. On success return {ok=true, status=http_code, body=body_string}. Implement classify_http(status, body) extracting JSON "error" and "hint" fields via regex; map 401→unauthorized, 503/409/504 per D-83. Do NOT include MessageBox or UI strings.
  </action>
  <verify>
    <automated>test -f lua/bridge_client.lua && grep -q 'function.*request' lua/bridge_client.lua && grep -q 'kind' lua/bridge_client.lua && grep -q 'X-Bridge-Key' lua/bridge_client.lua</automated>
  </verify>
  <acceptance_criteria>
    - bridge_client.lua exists with request() and structured error kinds
    - Auth header injection matches Phase 4 semantics
    - Module returns a table (require/loadfile compatible)
  </acceptance_criteria>
  <done>Transport layer extracted with structured results.</done>
</task>

<task type="auto">
  <name>Task 2: Add route helpers and parse functions (MTR-03, D-78, D-81)</name>
  <files>lua/bridge_client.lua</files>
  <read_first>lua/SekonicCalibrator.lua (bridge_fetch_measurement, bridge_check_status), sekonic-bridge/server.py (/status, /measure responses), 05-CONTEXT.md</read_first>
  <action>
    Add parse_measure_body(body) extracting cct, duv, cri, r9, optional tlci with tonumber; validate CCT 1667–25000 and Duv -0.05–0.05 (match existing monolith bounds); return validation kind on failure. Add parse_status_body(body) for connected, meter, device_configured, protocol_captured, trigger_discovered, auth_required, last_error (regex). Implement fetch_measurement(config), check_status(config), discover(config), capture(config), learn_trigger(config) calling request() with timeouts: status 5, discover 12, capture 35, measure 38, learn_trigger 120. Each returns {ok=true, data=...} or structured error. Use config.bridge_ip, config.bridge_port or 8765, config.bridge_api_key.
  </action>
  <verify>
    <automated>grep -q 'fetch_measurement' lua/bridge_client.lua && grep -q 'check_status' lua/bridge_client.lua && grep -q 'parse_measure_body' lua/bridge_client.lua && grep -q 'auth_required' lua/bridge_client.lua</automated>
  </verify>
  <acceptance_criteria>
    - All five route helpers exported
    - parse_status_body includes auth_required and last_error fields
    - Timeouts documented as local constants matching D-81 values
  </acceptance_criteria>
  <done>Route API complete for monolith wiring in Plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Register module in domain loader (ARCH-04, D-77)</name>
  <files>lua/SekonicCalibrator.lua</files>
  <read_first>lua/SekonicCalibrator.lua (load_domain_modules), lua/color_math.lua (module return pattern), 02-CONTEXT.md D-23</read_first>
  <action>
    Extend load_domain_modules() return table with bridge_client = try_require("bridge_client"). Add local bridge_client = domain.bridge_client at module scope. Replace _http_request body with thin delegate: local function _http_request(...) return bridge_client.request(...) end OR remove _http_request and leave call-site migration to Plan 02 — minimum for this task: loader registers bridge_client and existing plugin still loads (may keep temporary _http_request wrapper calling bridge_client.request to avoid breaking compile). Run lua5.4 tests/run.lua — must still pass 139+ (no regressions).
  </action>
  <verify>
    <automated>grep -q 'bridge_client' lua/SekonicCalibrator.lua && lua5.4 tests/run.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - load_domain_modules includes bridge_client
    - Host test suite green (139+ PASS)
    - No syntax errors in SekonicCalibrator.lua
  </acceptance_criteria>
  <done>Wave 1 complete — bridge_client module loadable; ARCH-04 seam in place.</done>
</task>

</tasks>

<verification>
- lua/bridge_client.lua exists with route helpers
- Domain loader registers module
- lua5.4 tests/run.lua regression green
</verification>

<success_criteria>
- ARCH-04: bridge_client.lua extracted with explicit timeouts and error types
- MTR-03: MeasurementRecord parse contract preserved in fetch_measurement
</success_criteria>

<output>
Create `.planning/phases/05-ma3-http-integration/05-01-SUMMARY.md` when done
</output>

## PLANNING COMPLETE
