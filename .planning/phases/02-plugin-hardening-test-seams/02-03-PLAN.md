---
phase: 02-plugin-hardening-test-seams
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - lua/goals.lua
  - lua/SekonicCalibrator.lua
  - test_color_math.lua
  - .planning/phases/02-plugin-hardening-test-seams/02-VALIDATION.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-03
  - ARCH-05
must_haves:
  truths:
    - goals_met and goal_status_str live in lua/goals.lua isolated from UI and bridge transport
    - goals_met host tests cover CCT ±150 K boundary, Duv tolerance, TLCI nil, GOAL_SKIP (D-32)
    - Dead base64_encode/decode and B64_* tables removed from entry file (D-33)
    - test_color_math.lua has zero inline domain duplicates; 126+ PASS + goals tests green
    - Entry file ~400–500 lines slimmer than ~2046 baseline; remaining ~1500+ lines stay until Phase 5–6 (D-21)
    - data/measurements/ directory left untouched — no create/delete in Phase 2 (D-35)
    - plugin.xml single ComponentLua unchanged; calibration logic on-console only (D-38, D-39)
  artifacts:
    - lua/goals.lua
    - lua/SekonicCalibrator.lua
    - test_color_math.lua
    - .planning/phases/02-plugin-hardening-test-seams/02-VALIDATION.md
  key_links:
    - from: lua/SekonicCalibrator.lua Section 6 auto-loop
      to: lua/goals.lua
      via: goals.goals_met(measured, session_goals)
      pattern: goals\.goals_met
    - from: test_color_math.lua goals section
      to: lua/goals.lua
      via: require("goals")
      pattern: goals_met
---

<objective>
Extract goals and assessment pure logic to `lua/goals.lua`, remove dead base64 code, add mandatory goals_met host tests, and run Phase 2 verification gate.

Purpose: Satisfies ARCH-03 (goals isolated from UI/transport), closes remaining ARCH-01/ARCH-05 coverage (no duplicates, single plugin, cleanup), and signs off Phase 2 per ROADMAP success criteria.
Output: goals.lua module; trimmed monolith; extended test suite; updated 02-VALIDATION.md statuses green.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-plugin-hardening-test-seams/02-CONTEXT.md
@.planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md
@.planning/phases/02-plugin-hardening-test-seams/02-VALIDATION.md
@lua/SekonicCalibrator.lua
@test_color_math.lua
@plugin.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lua/goals.lua and wire monolith (ARCH-03)</name>
  <files>lua/goals.lua, lua/SekonicCalibrator.lua</files>
  <read_first>lua/SekonicCalibrator.lua, .planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md, .planning/phases/02-plugin-hardening-test-seams/02-CONTEXT.md</read_first>
  <action>
    Per D-30, D-31, D-37: continue on claude/lighttune-main (post Wave 2). Create lua/goals.lua with return M exporting goals_met (from Section 2c ~L1213–1223), goal_status_str (from Section 2 ~L140–149), QUALITY, GOAL_MAX, GOAL_MIN, GOAL_SKIP, and optional named CCT_GOAL_TOLERANCE = 150. Move QUALITY and GOAL_* from Section 1 entry file. goals module must NOT require color_math or fixture_db (no circular deps). Extend load_domain_modules() to try_require("goals"). Remove Section 2c inline goals_met assignment; remove Section 3 forward decl `local goals_met` (keep bridge forward decls: _http_request, bridge_fetch_measurement, etc.). Update all call sites: goals.goals_met(measured, goals_table), goals.goal_status_str(...), goals.QUALITY. Assessment UI (show_assessment, rate_quality callers, goals_summary_line) stays in Section 3 but calls goals/color_math modules (D-31). Section 6 auto-loop uses goals.goals_met. Do NOT extract bridge_client (D-20).
  </action>
  <verify>
    <automated>test -f lua/goals.lua && grep -q 'goals_met' lua/goals.lua && grep -q 'goals.goals_met' lua/SekonicCalibrator.lua && ! grep -q 'goals_met = function' lua/SekonicCalibrator.lua</automated>
  </verify>
  <acceptance_criteria>
    - lua/goals.lua exports goals_met, goal_status_str, QUALITY, GOAL_*
    - No inline goals_met function definition in Section 2c
    - No `local goals_met` forward declaration in Section 3
    - show_assessment and related UI remain in monolith calling goals.*
    - Bridge forward-decl pattern for HTTP functions unchanged
  </acceptance_criteria>
  <done>Goals and assessment pure logic isolated from UI and bridge transport layers.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead base64 and stale comments (ARCH-05, D-33, D-36)</name>
  <files>lua/SekonicCalibrator.lua</files>
  <read_first>lua/SekonicCalibrator.lua, .planning/codebase/CONCERNS.md, .planning/phases/02-plugin-hardening-test-seams/02-CONTEXT.md</read_first>
  <action>
    Per D-33: delete base64_encode, base64_decode, B64_* lookup tables (~35 lines) from entry file — unused since community upload removal (0530fd2). Grep entire repo to confirm no callers remain. Per D-36: remove stale header comments referencing community upload or GitHub auto-upload if still present. Remove any base64 tests from test_color_math.lua if not already removed. Do NOT remove bridge Section 2c or session/mode constants (MODE_*, METER_*, STAR stay in Section 1 per D-20).
  </action>
  <verify>
    <automated>! grep -q 'base64_encode' lua/SekonicCalibrator.lua && ! grep -q 'B64_' lua/SekonicCalibrator.lua && lua5.4 test_color_math.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - No base64_encode, base64_decode, or B64_ symbols in SekonicCalibrator.lua
    - No community-upload claims in Lua file header
    - lua5.4 test_color_math.lua still exits 0
    - grep -c ComponentLua plugin.xml → 1
  </acceptance_criteria>
  <done>Dead code removed; entry file further slimmed without behavior change.</done>
</task>

<task type="auto">
  <name>Task 3: Add goals_met host tests (ARCH-03, D-32)</name>
  <files>test_color_math.lua</files>
  <read_first>test_color_math.lua, lua/goals.lua, .planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md</read_first>
  <action>
    Per D-32: extend test_color_math.lua with [goals_met] section (separate test_goals.lua optional but not required). Add local goals = require("goals") at top alongside color_math and fixture_db. Test cases minimum: (1) all goals met at exact targets; (2) CCT boundary — pass at ±150 K, fail at ±151 K; (3) Duv boundary at QUALITY.DUV.acceptable; (4) GOAL_SKIP mode ignored; (5) TLCI nil when goal set — document expected behavior matching monolith; (6) GOAL_MIN / GOAL_MAX modes for CRI/R9. Use assert_equal on goals.goals_met boolean return. Run full suite.
  </action>
  <verify>
    <automated>grep -q 'goals_met' test_color_math.lua && grep -q 'require("goals")' test_color_math.lua && lua5.4 test_color_math.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - test_color_math.lua requires goals module
    - At least 5 distinct goals_met test cases including CCT boundary and GOAL_SKIP
    - lua5.4 test_color_math.lua exits 0 with goals section PASS
    - Zero FAIL in test output
  </acceptance_criteria>
  <done>goals_met regression coverage exists before Phase 5 auto-loop integration.</done>
</task>

<task type="auto">
  <name>Task 4: Phase 2 verification gate and validation update</name>
  <files>.planning/phases/02-plugin-hardening-test-seams/02-VALIDATION.md, lua/SekonicCalibrator.lua, plugin.xml, test_color_math.lua</files>
  <read_first>.planning/phases/02-plugin-hardening-test-seams/02-VALIDATION.md, .planning/ROADMAP.md, .planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md</read_first>
  <action>
    Run Phase 2 sign-off gates from 02-VALIDATION.md and ROADMAP: (1) lua5.4 test_color_math.lua — 126+ color-math PASS + goals tests, zero FAIL; (2) ! grep -q "Inline copies" test_color_math.lua; (3) grep -c ComponentLua plugin.xml → 1; (4) ! grep -q base64_encode lua/SekonicCalibrator.lua; (5) test -f lua/color_math.lua lua/fixture_db.lua lua/goals.lua; (6) wc -l lua/SekonicCalibrator.lua — expect ~1550–1650 lines (down ~400–500 from ~2046); (7) load_domain_modules present with color_math, fixture_db, goals. Update 02-VALIDATION.md Per-Task Verification Map statuses to green for all listed rows (02-01-01 through 02-03-03) and mark Validation Sign-Off checkboxes complete. Set wave_0_complete true when lua5.4 available. Note MA3 console require verification deferred Phase 7; dofile fallback must remain. Commit product changes to claude/lighttune-main; validation doc may update on planning branch per D-15 (Phase 1 planning-branch convention).
  </action>
  <verify>
    <automated>lua5.4 test_color_math.lua && test -f lua/color_math.lua && test -f lua/fixture_db.lua && test -f lua/goals.lua && grep -c ComponentLua plugin.xml | grep -q '^1$' && ! grep -q 'Inline copies' test_color_math.lua</automated>
  </verify>
  <acceptance_criteria>
    - All Phase 2 requirement IDs satisfied: ARCH-01, ARCH-02, ARCH-03, ARCH-05, DB-01 (via Waves 1–3)
    - lua5.4 test_color_math.lua exits 0
    - Domain trio modules exist and entry file loads all three
    - plugin.xml single ComponentLua unchanged
    - 02-VALIDATION.md task statuses updated to green
    - Entry line count reduced approximately 400–500 lines from baseline
  </acceptance_criteria>
  <done>Phase 2 complete — host-testable domain modules extracted; single-plugin artifact preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| goals_met → auto-loop gate | Pure function; no I/O; Phase 5 depends on correct boundary logic |
| Removed base64 | Eliminates unused encode path; no secret handling was present |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-04 | Tampering | goals_met thresholds | low | mitigate | Host boundary tests lock CCT ±150 K and Duv tolerance before Phase 5 auto-loop |
| T-2-02 | Denial of service | Module loader on MA3 | medium | mitigate | dofile fallback retained; Phase 7 UAT |

</threat_model>

<verification>
- lua/goals.lua with goals_met and goal_status_str
- goals_met boundary tests pass
- base64 removed; no Inline copies in tests
- lua5.4 test_color_math.lua full green
- wc -l lua/SekonicCalibrator.lua ~1550–1650
- 02-VALIDATION.md updated
</verification>

<success_criteria>
- ARCH-03: goals isolated from UI and transport
- ARCH-01: no test duplication; shared modules only
- ARCH-05: single deployable plugin; domain trio as sibling modules
- Phase 2 ROADMAP success criteria 1–4 all TRUE
- Ready for Phase 3 CI on shared modules
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Location |
|----------|------|----------|
| goals module | Lua module (`return M`) | lua/goals.lua |
| goals_met, goal_status_str | functions | lua/goals.lua |
| QUALITY, GOAL_MAX, GOAL_MIN, GOAL_SKIP, CCT_GOAL_TOLERANCE | constants/tables | lua/goals.lua |
| goals_met host test section | test cases | test_color_math.lua |
| Complete load_domain_modules (color_math + fixture_db + goals) | loader | lua/SekonicCalibrator.lua |
| Trimmed monolith (~1550–1650 lines) | entry file | lua/SekonicCalibrator.lua |
| Updated validation status | doc | 02-VALIDATION.md |

<output>
Create `.planning/phases/02-plugin-hardening-test-seams/02-03-SUMMARY.md` when done
</output>
