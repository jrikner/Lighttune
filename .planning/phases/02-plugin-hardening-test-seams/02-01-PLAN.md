---
phase: 02-plugin-hardening-test-seams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lua/color_math.lua
  - lua/SekonicCalibrator.lua
  - test_color_math.lua
autonomous: true
requirements:
  - ARCH-01
  - ARCH-05
must_haves:
  truths:
    - Color math lives in lua/color_math.lua as return-M table shared by plugin and host tests (D-19)
    - test_color_math.lua at repo root requires color_math — no inline Section 2 duplicate (D-25, D-34)
    - SekonicCalibrator.lua loads color_math via require+dofile loader; plugin.xml still declares single ComponentLua entry (D-22, D-38)
    - Calibration intelligence stays on-console — no logic moves to Pi (D-39)
    - lua5.4 test_color_math.lua passes 126+ assertions with zero FAIL after extraction
  artifacts:
    - lua/color_math.lua
    - lua/SekonicCalibrator.lua
    - test_color_math.lua
  key_links:
    - from: test_color_math.lua
      to: lua/color_math.lua
      via: package.path ./lua/?.lua + require("color_math")
      pattern: color_math.cct_to_xy
    - from: lua/SekonicCalibrator.lua load_domain_modules
      to: lua/color_math.lua
      via: GetPath PluginLibrary + require/dofile fallback
      pattern: try_require\("color_math"\)
---

<objective>
Extract Section 2 pure color math into `lua/color_math.lua`, add the module loader to the entry file, and eliminate duplicated inline color math in `test_color_math.lua`.

Purpose: Satisfies ARCH-01 (testable shared module) and ARCH-05 foundation (single deployable plugin with sibling modules loaded at runtime per D-19, D-22–D-25, D-38).
Output: New `color_math.lua`; entry file Section 2 replaced with `color_math.*` calls; host tests green via `require`.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-plugin-hardening-test-seams/02-CONTEXT.md
@.planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md
@.planning/codebase/CONCERNS.md
@lua/SekonicCalibrator.lua
@test_color_math.lua
@plugin.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lua/color_math.lua from Section 2 (ARCH-01)</name>
  <files>lua/color_math.lua</files>
  <read_first>lua/SekonicCalibrator.lua, .planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md, test_color_math.lua</read_first>
  <action>
    Per D-19, D-24, and RESEARCH Function Inventory: checkout claude/lighttune-main (post Phase 1 baseline). Create lua/color_math.lua by moving Section 2 function bodies verbatim (no behavior change): cct_to_xy, xy_to_uvp, uvp_to_xy, apply_duv_correction, get_correction, xy_to_rgb, rgb_to_hsb, rate_quality, rate_duv, gel_hint. Move supporting constants from Section 1: CCT_MIN, CCT_MAX, DUV_MIN, DUV_MAX, GEL_STEPS. Do NOT move goal_status_str, goals_met, QUALITY, GOAL_* (Wave 3 goals.lua), base64 helpers (Wave 3 delete), or fixture DB symbols (Wave 2). Module must return local M table with all public functions and constants exported; no MA3 API calls at module top level; no _G pollution. Add brief module header documenting Section 2 origin and return-M contract.
  </action>
  <verify>
    <automated>test -f lua/color_math.lua && grep -q 'return M' lua/color_math.lua && grep -q 'cct_to_xy' lua/color_math.lua && grep -q 'get_correction' lua/color_math.lua</automated>
  </verify>
  <acceptance_criteria>
    - lua/color_math.lua exists with return M export table
    - All Section 2 pure math functions listed in RESEARCH inventory are present on M
    - CCT_MIN/CCT_MAX/DUV_MIN/DUV_MAX and GEL_STEPS exported on M
    - No GetPath, MessageBox, Cmd, or other MA3 globals referenced in color_math.lua
    - goal_status_str and goals_met are NOT in color_math.lua (deferred to Wave 3)
  </acceptance_criteria>
  <done>color_math.lua is a self-contained pure module ready for require from host and console.</done>
</task>

<task type="auto">
  <name>Task 2: Add module loader and wire SekonicCalibrator.lua (ARCH-05)</name>
  <files>lua/SekonicCalibrator.lua</files>
  <read_first>lua/SekonicCalibrator.lua, .planning/phases/02-plugin-hardening-test-seams/02-CONTEXT.md, .planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md</read_first>
  <action>
    Per D-22, D-23, D-38: keep lua/SekonicCalibrator.lua as sole ComponentLua entry — do NOT add plugin.xml components or rename to main.lua. Insert load_domain_modules() immediately after file header comment per RESEARCH Pattern 1: resolve plugin_dir via pcall(GetPath(Enums.PathType.PluginLibrary)); prepend plugin_dir .. "/lua/?.lua" to package.path; try_require(name) uses pcall(require) then dofile(plugin_dir .. "/lua/" .. name .. ".lua") fallback. Wave 1 loads color_math only — fixture_db and goals added in Waves 2–3. Remove Section 2 function bodies and moved constants from entry file; replace all call sites with color_math.* (e.g. color_math.cct_to_xy, color_math.CCT_MIN). Preserve Section 2 banner comment as pointer to color_math.lua. Bridge Section 2c range checks must reference color_math.CCT_MIN etc. Do NOT extract Section 2b, 2c, or touch base64 yet. Commit to claude/lighttune-main per D-37.
  </action>
  <verify>
    <automated>grep -q 'load_domain_modules' lua/SekonicCalibrator.lua && grep -q 'color_math' lua/SekonicCalibrator.lua && ! grep -q 'local function cct_to_xy' lua/SekonicCalibrator.lua && grep -c ComponentLua plugin.xml | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - load_domain_modules() present at top of SekonicCalibrator.lua with require+dofile fallback
    - No local function cct_to_xy (or other extracted Section 2 bodies) remains in entry file
    - plugin.xml declares exactly one ComponentLua → lua/SekonicCalibrator.lua
    - Section 2c bridge code still present and references color_math constants where needed
    - Entry file line count reduced from ~2046 baseline (partial reduction expected in Wave 1)
  </acceptance_criteria>
  <done>Monolith loads color_math as sibling module while remaining single deployable plugin artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor test_color_math.lua to require color_math (ARCH-01)</name>
  <files>test_color_math.lua</files>
  <read_first>test_color_math.lua, lua/color_math.lua, .planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md</read_first>
  <action>
    Per D-25 and RESEARCH Pattern 6: delete inline Section 2 duplicate block (~lines 57–302 or equivalent "Inline copies" region). At top of test file set package.path = package.path .. ";./lua/?.lua" and local color_math = require("color_math"). Replace all direct calls (cct_to_xy, get_correction, rate_duv, etc.) with color_math.* equivalents. Keep test harness (assert_near, assert_equal) and Section 2b fixture DB tests unchanged for Wave 2 — those may still inline or call local copies until 02-02. Remove any base64-related test sections only if they exist and base64 is still dead code (optional in Wave 1; mandatory removal in Wave 3). Run lua5.4 test_color_math.lua — all color-math sections must PASS with zero FAIL.
  </action>
  <verify>
    <automated>command -v lua5.4 && lua5.4 test_color_math.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - test_color_math.lua contains require("color_math") and package.path bootstrap
    - No "Inline copies" or duplicated Section 2 function definitions in test file
    - lua5.4 test_color_math.lua exits 0
    - Test output shows 126+ PASS (or documented baseline count) and zero FAIL for color-math sections
  </acceptance_criteria>
  <done>Host tests share single color_math source — drift risk for Section 2 eliminated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host test runner → lua/ modules | Local filesystem only; no network |
| MA3 console → sibling .lua files | Plugin dir resolved via GetPath; dofile fallback if require fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | fixture_log.json string fields | medium | defer | Quote-escape hardening in Wave 2 (02-02); not in Wave 1 scope |
| T-2-02 | Denial of service | Module loader failure on MA3 | medium | mitigate | dofile fallback per D-23; Phase 7 UAT verifies require path |

</threat_model>

<verification>
- lua/color_math.lua exists with return M contract
- lua5.4 test_color_math.lua exits 0
- grep -c ComponentLua plugin.xml → 1
- No local function cct_to_xy in SekonicCalibrator.lua
</verification>

<success_criteria>
- ARCH-01: color math extracted and shared by plugin + host tests
- ARCH-05: single plugin.xml entry; color_math loaded as sibling module
- 126+ color-math assertions green after Wave 1
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Location |
|----------|------|----------|
| color_math module | Lua module (`return M`) | lua/color_math.lua |
| load_domain_modules loader | function | lua/SekonicCalibrator.lua (top) |
| Exported symbols: cct_to_xy, xy_to_uvp, uvp_to_xy, apply_duv_correction, get_correction, xy_to_rgb, rgb_to_hsb, rate_quality, rate_duv, gel_hint | functions | lua/color_math.lua |
| Exported constants: CCT_MIN, CCT_MAX, DUV_MIN, DUV_MAX, GEL_STEPS | scalars/tables | lua/color_math.lua |
| Refactored host test bootstrap | test file | test_color_math.lua |

<output>
Create `.planning/phases/02-plugin-hardening-test-seams/02-01-SUMMARY.md` when done
</output>
