---
phase: 02-plugin-hardening-test-seams
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - lua/fixture_db.lua
  - lua/SekonicCalibrator.lua
  - test_color_math.lua
autonomous: true
requirements:
  - ARCH-02
  - DB-01
must_haves:
  truths:
    - Fixture DB logic lives in lua/fixture_db.lua with hardened JSON encode/decode (not regex-only public API)
    - String fields escape quotes, backslashes, and control chars; malformed records skipped with visible error not silent drop
    - append-only fixture_log.json semantics and best_* flag recompute unchanged (DB-01)
    - Entry file Section 2b removed; Section 5 I/O calls fixture_db.*; tests require fixture_db module
  artifacts:
    - lua/fixture_db.lua
    - lua/SekonicCalibrator.lua
    - test_color_math.lua
  key_links:
    - from: lua/SekonicCalibrator.lua save_fixture_log_local
      to: lua/fixture_db.lua
      via: fixture_db.json_encode_db_array / json_parse_db_array
      pattern: fixture_db\.
    - from: test_color_math.lua
      to: lua/fixture_db.lua
      via: require("fixture_db")
      pattern: fixture_db.recompute_best_flags
---

<objective>
Extract Section 2b fixture database logic into `lua/fixture_db.lua`, harden JSON parse/encode for the known flat-array schema, and wire the monolith and host tests to the shared module.

Purpose: Satisfies ARCH-02 (hardened JSON module) and DB-01 (append-only best-value flags) per D-26–D-29, D-27 internal regex fallback only.
Output: New fixture_db.lua; entry file ~130 lines slimmer; tests cover quote-in-make roundtrip and best_* semantics via require.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-plugin-hardening-test-seams/02-CONTEXT.md
@.planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md
@.planning/codebase/CONCERNS.md
@lua/SekonicCalibrator.lua
@test_color_math.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lua/fixture_db.lua from Section 2b (ARCH-02, DB-01)</name>
  <files>lua/fixture_db.lua</files>
  <read_first>lua/SekonicCalibrator.lua, .planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md, test_color_math.lua</read_first>
  <action>
    Per D-19, D-24, D-37: continue on claude/lighttune-main (post Wave 1). Move Section 2b functions verbatim into lua/fixture_db.lua as return M table: json_encode_db_record, json_encode_db_array, json_parse_db_array, recompute_best_flags, append_fixture_record, sort_fixture_records, find_best_for_fixture. Move json_get_str, json_get_num, json_get_bool as local (module-private) helpers. Document schema in module header (make, model, kelvin, date, contributor, cct, duv, cri, r9, tlci?, best_*). append_fixture_record may use os.date — no MA3 API. Export public API on M with same function names as monolith for minimal call-site churn. Do NOT harden JSON yet in this task — move first, behavior unchanged.
  </action>
  <verify>
    <automated>test -f lua/fixture_db.lua && grep -q 'return M' lua/fixture_db.lua && grep -q 'recompute_best_flags' lua/fixture_db.lua && grep -q 'append_fixture_record' lua/fixture_db.lua</automated>
  </verify>
  <acceptance_criteria>
    - lua/fixture_db.lua exists with all Section 2b public functions on M
    - recompute_best_flags and append_fixture_record present and match monolith signatures
    - No MA3 globals in fixture_db.lua
    - Module header documents append-only array schema and best_* semantics
  </acceptance_criteria>
  <done>Fixture DB pure logic extracted to testable module (pre-hardening move).</done>
</task>

<task type="auto">
  <name>Task 2: Harden JSON encode/decode in fixture_db.lua (ARCH-02, T-2-01)</name>
  <files>lua/fixture_db.lua, test_color_math.lua</files>
  <read_first>.planning/phases/02-plugin-hardening-test-seams/02-CONTEXT.md, .planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md, .planning/codebase/CONCERNS.md</read_first>
  <action>
    Per D-26, D-27, D-29 and RESEARCH Pattern 4: implement json_escape_str applying to make, model, contributor, date in json_encode_db_record — escape \, ", and control chars (\n \r \t \x00-\x1f). Harden json_parse_db_array: primary structured block scan with escaped-string-aware extraction; on per-record failure increment skipped counter and collect error string — do NOT silently omit without trace; per-block legacy regex fallback (json_get_*) internal only, not exported second API. Add host test: roundtrip record with make containing embedded double-quote (e.g. Acme "Pro" 600); assert parse returns record and skipped == 0. Add test: malformed {...} block yields skipped >= 1. Preserve append-only array shape and best_* flag semantics unchanged (DB-01). Do NOT touch bridge Section 2c JSON regex (D-28).
  </action>
  <verify>
    <automated>grep -q 'json_escape' lua/fixture_db.lua && lua5.4 test_color_math.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - json_encode_db_record escapes " and \ in string fields
    - json_parse_db_array returns skipped count or equivalent error reporting on malformed records
    - Legacy regex helpers remain module-private fallback only (D-27)
    - New test with quote in make field passes
    - lua5.4 test_color_math.lua exits 0 including fixture_db sections
  </acceptance_criteria>
  <done>Fixture DB JSON hardened against operator fixture names with special characters.</done>
</task>

<task type="auto">
  <name>Task 3: Wire monolith loader + tests to fixture_db (ARCH-02, DB-01)</name>
  <files>lua/SekonicCalibrator.lua, test_color_math.lua</files>
  <read_first>lua/SekonicCalibrator.lua, lua/fixture_db.lua, test_color_math.lua, .planning/phases/02-plugin-hardening-test-seams/02-RESEARCH.md</read_first>
  <action>
    Extend load_domain_modules() in SekonicCalibrator.lua to try_require("fixture_db") alongside color_math (D-23). Remove Section 2b function bodies from entry file; update Section 5 save_fixture_log_local, load_fixture_log_local, and history viewer to call fixture_db.json_parse_db_array / json_encode_db_array / recompute_best_flags / find_best_for_fixture. When parse returns skipped > 0, surface warning via MessageBox (acceptable UX per RESEARCH). Delete inline Section 2b duplicate in test_color_math.lua if present; add local fixture_db = require("fixture_db") and route fixture DB tests through fixture_db.*. Run lua5.4 test_color_math.lua after wiring. Commit to claude/lighttune-main. GSD planning artifacts stay on planning branch per D-15 (Phase 1 planning-branch convention) and D-37.
  </action>
  <verify>
    <automated>grep -q 'fixture_db' lua/SekonicCalibrator.lua && ! grep -q 'local function json_encode_db_record' lua/SekonicCalibrator.lua && lua5.4 test_color_math.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - load_domain_modules loads fixture_db via require/dofile
    - No Section 2b function bodies remain in SekonicCalibrator.lua
    - recompute_best_flags and append_fixture_record tests pass via require
    - DB-01 append-only behavior preserved — append_fixture_record does not delete history
    - lua5.4 test_color_math.lua exits 0
  </acceptance_criteria>
  <done>Monolith and host tests share fixture_db module with hardened JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator fixture names → fixture_log.json | Untrusted text input; may contain quotes, backslashes, Unicode |
| Corrupt local DB file → plugin parse | Local filesystem; no remote attacker |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | fixture_log.json string fields | medium | mitigate | json_escape_str on encode; escaped-string-aware parse; skip malformed with operator-visible warning |
| T-2-03 | Information disclosure | Skipped record errors in MessageBox | low | accept | Errors describe corrupt local records only; no secrets in fixture_log.json |

</threat_model>

<verification>
- lua/fixture_db.lua with hardened encode/parse
- Quote-in-make roundtrip test passes
- recompute_best_flags semantics unchanged
- lua5.4 test_color_math.lua exits 0
- Bridge Section 2c JSON parsing untouched
</verification>

<success_criteria>
- ARCH-02: fixture DB module with structured JSON hardening
- DB-01: append-only best_* flag semantics preserved and tested
- Entry file calls fixture_db.* exclusively for Section 2b logic
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Location |
|----------|------|----------|
| fixture_db module | Lua module (`return M`) | lua/fixture_db.lua |
| json_escape_str | private helper | lua/fixture_db.lua |
| Public API: json_encode_db_array, json_parse_db_array, recompute_best_flags, append_fixture_record, sort_fixture_records, find_best_for_fixture | functions | lua/fixture_db.lua |
| Parse skipped/error reporting | behavior | json_parse_db_array return or side channel |
| Quote-in-make host test | test case | test_color_math.lua |
| Extended load_domain_modules | loader | lua/SekonicCalibrator.lua |

<output>
Create `.planning/phases/02-plugin-hardening-test-seams/02-02-SUMMARY.md` when done
</output>
