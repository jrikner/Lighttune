---
phase: 01-canonical-merge-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - lua/SekonicCalibrator.lua
  - plugin.xml
  - README.md
  - data/config.json.example
  - .gitignore
  - sekonic-bridge/server.py
autonomous: true
requirements:
  - BASE-02
  - BASE-03
must_haves:
  truths:
    - Unified version label v0.5.0-replan appears in Lua header, README, plugin.xml, and bridge /status JSON
    - README describes only implemented behavior — no false community-upload, console curl/unzip, or on-disk GDTF claims
    - README and config example state config.json lives at plugin root next to plugin.xml
    - load_config() path in code matches documented copy instructions
    - Plugin-root config.json is gitignored; example remains at data/config.json.example
  artifacts:
    - README.md
    - plugin.xml
    - data/config.json.example
    - .gitignore
  key_links:
    - from: README config section
      to: lua/SekonicCalibrator.lua load_config()
      via: plugin root config.json path
      pattern: GetPath PluginLibrary + config.json
---

<objective>
Align version strings to v0.5.0-replan and perform full README/config honesty pass so docs match post-cherry-pick code.

Purpose: Satisfies BASE-02 and BASE-03 (D-07 through D-13, D-17, D-18) after code lands in plan 01-01.
Output: Single alignment commit updating manifests, README, config example comments, .gitignore, bridge status version.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-canonical-merge-baseline/01-CONTEXT.md
@.planning/phases/01-canonical-merge-baseline/01-RESEARCH.md
@lua/SekonicCalibrator.lua
@README.md
@plugin.xml
@data/config.json.example
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply v0.5.0-replan version label everywhere (D-07, D-08, D-09)</name>
  <files>lua/SekonicCalibrator.lua, plugin.xml, README.md, sekonic-bridge/server.py</files>
  <read_first>lua/SekonicCalibrator.lua, plugin.xml, README.md, sekonic-bridge/server.py, .planning/phases/01-canonical-merge-baseline/01-CONTEXT.md</read_first>
  <action>
    Per D-07 and D-08: set unified interim version v0.5.0-replan (brownfield replan milestone, NOT v1.0.0 per D-09). Update Lua file header comment to SekonicCalibrator v0.5.0-replan. Update README title/subtitle and any version callouts to v0.5.0-replan. Set plugin.xml Plugin Version attribute to 0.5.0-replan; if MA3 rejects non-semver suffix per discretion, fallback to Version="0.5.0" with v0.5.0-replan retained in Lua header and README only — document chosen format in commit message. In sekonic-bridge/server.py /status JSON response, change version field from 1.0.0 to 0.5.0-replan (or 1.0.0-replan if bridge semver is kept separate — pick one and align README bridge section). Do NOT set v1.0.0 anywhere.
  </action>
  <verify>
    <automated>grep -n 'v0.5.0-replan' lua/SekonicCalibrator.lua README.md | wc -l | grep -qv '^0$' && grep -n 'Version=' plugin.xml | grep -E '0\.5\.0'</automated>
  </verify>
  <acceptance_criteria>
    - lua/SekonicCalibrator.lua header contains v0.5.0-replan
    - README.md header or subtitle contains v0.5.0-replan
    - plugin.xml Version attribute is 0.5.0-replan or 0.5.0 with replan noted in README/Lua
    - sekonic-bridge/server.py /status version field no longer reads 1.0.0 alone
    - No file contains v1.0.0 as product version string
  </acceptance_criteria>
  <done>Version drift eliminated; all declared versions reflect replan milestone per D-08.</done>
</task>

<task type="auto">
  <name>Task 2: README full honesty pass (D-10, D-11, D-12, D-13)</name>
  <files>README.md</files>
  <read_first>README.md, lua/SekonicCalibrator.lua, .planning/phases/01-canonical-merge-baseline/01-CONTEXT.md, .planning/phases/01-canonical-merge-baseline/01-RESEARCH.md, .planning/codebase/CONCERNS.md</read_first>
  <action>
    Per D-10 through D-13: rewrite README to describe only post-cherry-pick behavior. Remove or rewrite false claims: GitHub community upload paths, automated upload from console, curl/unzip as GrandMA3 console requirements, on-disk GDTF file reading under gma3_library paths — code uses Patch API via DataPool Groups FixtureType only. Retain or strengthen truthful Fixture Capability Detection section (Patch API). Add remote bridge workflow: Pi on show LAN, bridge_ip and bridge_port in config.json, Bridge Status menu, manual Sekonic entry always available, curl examples run on Pi terminal not console. Keep HID/trigger-discovery wizard docs where they match current v0.5 code — do NOT remove wizard docs as deferred rename is Phase 4–6. Do NOT add claims for deferred features (API key auth, module split, community sync).
  </action>
  <verify>
    <automated>grep -v '^#' README.md | grep -ci 'community_upload\|gma3_library/gdtf\|curl.*console' | grep -q '^0$' && grep -ci 'Patch API\|bridge_ip\|plugin root' README.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - README has no lines matching community upload or automated GitHub upload from console
    - README has no on-disk GDTF path under gma3_library
    - README contains Patch API or FixtureType capability wording
    - README documents bridge_ip, bridge_port, Pi LAN workflow, and manual meter fallback
    - README states curl setup commands run on Pi terminal
  </acceptance_criteria>
  <done>README is truthful for v0.5.0-replan codebase per BASE-02.</done>
</task>

<task type="auto">
  <name>Task 3: Config path docs and .gitignore alignment (D-17, D-18, BASE-03)</name>
  <files>data/config.json.example, README.md, .gitignore</files>
  <read_first>lua/SekonicCalibrator.lua, data/config.json.example, README.md, .gitignore, .planning/phases/01-canonical-merge-baseline/01-RESEARCH.md</read_first>
  <action>
    Per D-17 and D-18: confirm load_config() resolves GetPath(Enums.PathType.PluginLibrary) plus config.json at plugin root — do not change code path unless cherry-pick broke it. Update data/config.json.example with leading _comment key instructing copy to config.json at plugin root same folder as plugin.xml NOT under data/. Update README Configuration section to same copy path; remove any instruction to copy to data/config.json for runtime config. Fix .gitignore: add plugin-root config.json entry (e.g. /config.json or config.json at repo plugin root); keep data/config.json.example tracked; retain data/fixture_log.json ignore if present. Runtime data paths like data/fixture_log.json stay under data/ per D-18.
  </action>
  <verify>
    <automated>grep -A8 'local function load_config' lua/SekonicCalibrator.lua | grep -q 'config.json' && grep -q 'plugin root' data/config.json.example README.md && grep config.json .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - load_config() in lua/SekonicCalibrator.lua opens config.json at plugin library root not data/config.json
    - data/config.json.example _comment directs copy to plugin root config.json
    - README Configuration section matches plugin root path
    - .gitignore ignores runtime config.json at plugin root
    - grep for data/config.json as runtime copy target in README returns no matches
  </acceptance_criteria>
  <done>Config path documented consistently between README, example file, .gitignore, and load_config() per BASE-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → config.json | Local file may hold bridge_ip; must not enter git |
| Docs → operator expectations | False README claims cause misconfiguration on show LAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-03 | Information disclosure | config.json | low | mitigate | Add plugin-root config.json to .gitignore; README warns no secrets in git |
| T-1-04 | Spoofing | bridge_ip misconfig | medium | accept | Document bridge_ip validation in README; operator confirms Bridge Status before show |
| T-1-05 | Information disclosure | Unauthenticated /measure | medium | accept | README show-network section documents trusted VLAN; Phase 4 adds bridge_api_key |

</threat_model>

<verification>
- grep version strings across Lua, README, plugin.xml, server.py
- grep audit for false README claims
- grep config path consistency
</verification>

<success_criteria>
- v0.5.0-replan unified labeling
- README honest for implemented features
- Config path BASE-03 satisfied
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Location |
|----------|------|----------|
| v0.5.0-replan version strings | metadata | lua/SekonicCalibrator.lua, README.md, plugin.xml, sekonic-bridge/server.py |
| _comment config copy directive | JSON key | data/config.json.example |
| plugin-root config.json gitignore rule | git config | .gitignore |
| Bridge workflow documentation | prose | README.md |

<output>
Create `.planning/phases/01-canonical-merge-baseline/01-02-SUMMARY.md` when done
</output>
