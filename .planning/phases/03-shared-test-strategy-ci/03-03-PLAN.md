---
phase: 03-shared-test-strategy-ci
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - .github/workflows/ci.yml
  - README.md
  - .planning/phases/03-shared-test-strategy-ci/03-VALIDATION.md
autonomous: true
requirements:
  - TST-04
must_haves:
  truths:
    - GitHub Actions workflow runs on push and pull_request to claude/lighttune-main and cursor/** branches (D-50)
    - CI job host-lua installs lua5.4 via apt and runs lua5.4 tests/run.lua with non-zero exit on FAIL (D-51, D-52)
    - CI job bridge-pytest uses Python 3.12 venv, installs runtime + dev requirements, runs pytest tests/test_bridge_routes.py (D-51, D-52)
    - No secrets, Pi hardware, USB devices, or MA3 runner in workflow (D-53)
    - Workflow reports pass/fail status on PRs; both jobs must pass for green CI
    - README documents CI badge or "CI runs on push" section pointing to workflow
  artifacts:
    - .github/workflows/ci.yml
    - .planning/phases/03-shared-test-strategy-ci/03-VALIDATION.md
  key_links:
    - from: .github/workflows/ci.yml host-lua job
      to: tests/run.lua
      via: lua5.4 tests/run.lua
      pattern: tests/run\.lua
    - from: .github/workflows/ci.yml bridge-pytest job
      to: tests/test_bridge_routes.py
      via: pytest tests/test_bridge_routes.py
      pattern: test_bridge_routes
---

<objective>
Add a GitHub Actions CI workflow that runs the Wave 1 host Lua test runner and Wave 2 bridge pytest suite on every push/PR, and sign off Phase 3 with a validation gate.

Purpose: Satisfies TST-04 (CI workflow runs host Lua tests and bridge pytest on push) and completes ROADMAP Phase 3 success criteria. Regressions on shared modules or bridge routes are caught before Phase 4–5 feature work.
Output: `.github/workflows/ci.yml`; updated README CI section; `03-VALIDATION.md` with green sign-off.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md
@.planning/phases/02-plugin-hardening-test-seams/02-VALIDATION.md
@tests/run.lua
@tests/test_bridge_routes.py
@sekonic-bridge/requirements-dev.txt
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow (TST-04, D-50–D-53)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md, tests/run.lua, tests/test_bridge_routes.py, sekonic-bridge/requirements-dev.txt, .planning/phases/02-plugin-hardening-test-seams/02-VALIDATION.md</read_first>
  <action>
    Per D-50, D-51, D-52, D-53: create .github/workflows/ci.yml with:
    - Triggers: push and pull_request to branches claude/lighttune-main and cursor/** (use branch filter patterns GitHub Actions supports — e.g. `branches: [claude/lighttune-main, 'cursor/**']` or equivalent push/PR config).
    - Job `host-lua`: runs-on ubuntu-latest; steps: checkout; `sudo apt-get update && sudo apt-get install -y lua5.4`; run `lua5.4 tests/run.lua` from repo root; fail job on non-zero exit.
    - Job `bridge-pytest`: runs-on ubuntu-latest; steps: checkout; setup-python 3.12; create venv; `pip install -r sekonic-bridge/requirements.txt -r sekonic-bridge/requirements-dev.txt`; run `pytest tests/test_bridge_routes.py -v --tb=short`; optional pytest timeout env if MockMeter sleep is slow.
    - Jobs may run in parallel (no job dependency required unless apt contention — parallel preferred).
    - No secrets, no services block for USB, no GrandMA3, no curl to external hosts beyond pip/apt.
    - Workflow name: "CI" or "Lighttune CI". Add workflow_dispatch optional for manual runs.
    Validate YAML locally if actionlint available; otherwise manual review. Push to claude/lighttune-main per D-54.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q 'tests/run.lua' .github/workflows/ci.yml && grep -q 'pytest' .github/workflows/ci.yml && grep -q 'lua5.4' .github/workflows/ci.yml && grep -q '3.12' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists and is valid YAML
    - host-lua job runs lua5.4 tests/run.lua
    - bridge-pytest job runs pytest on tests/test_bridge_routes.py with Python 3.12
    - Triggers include claude/lighttune-main and cursor/** branches
    - No secrets or hardware dependencies in workflow
    - Both jobs fail fast on test failure (non-zero exit)
  </acceptance_criteria>
  <done>TST-04 core deliverable — automated CI gate for host Lua and bridge pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Update README and document local CI parity (TST-04)</name>
  <files>README.md</files>
  <read_first>README.md, .github/workflows/ci.yml, .planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md</read_first>
  <action>
    Update README.md Testing or Development section to document:
    - Primary host command: `lua5.4 tests/run.lua` (alias: `lua5.4 test_color_math.lua`)
    - Bridge tests: venv + `pytest tests/test_bridge_routes.py -v`
    - CI: GitHub Actions runs both on push/PR (name workflow file path `.github/workflows/ci.yml`)
    - Expected host baseline: 133+ PASS, 0 FAIL (note 126+ ROADMAP minimum)
    Do NOT add false claims about MA3 or hardware CI. Keep concise — match README honesty standards from BASE-02.
  </action>
  <verify>
    <automated>grep -q 'tests/run.lua' README.md && grep -q 'pytest' README.md && grep -q 'ci.yml' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README documents tests/run.lua, pytest command, and CI workflow
    - No claims about console or Pi hardware in CI section
    - Pass count expectation documented (133+ or 126+ minimum)
  </acceptance_criteria>
  <done>Operators and contributors can reproduce CI locally from README.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 3 verification gate and validation doc (TST-01–04)</name>
  <files>.planning/phases/03-shared-test-strategy-ci/03-VALIDATION.md</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md, .planning/phases/02-plugin-hardening-test-seams/02-VALIDATION.md, .github/workflows/ci.yml</read_first>
  <action>
    Create .planning/phases/03-shared-test-strategy-ci/03-VALIDATION.md mirroring 02-VALIDATION.md structure:
    - Per-plan verification map for 03-01, 03-02, 03-03 tasks with pass/fail status.
    - Phase 3 sign-off gates:
      (1) lua5.4 tests/run.lua — 133+ PASS, 0 FAIL (TST-01)
      (2) pytest tests/test_bridge_routes.py — all pass (TST-02)
      (3) tests/fixtures/ contains four golden files (TST-03)
      (4) .github/workflows/ci.yml present and both jobs defined (TST-04)
      (5) lua5.4 test_color_math.lua backward compat exits 0
      (6) sekonic-bridge/requirements-dev.txt separate from runtime requirements.txt
    - Run local verification commands and record results in validation doc.
    - Mark all TST-01–04 requirement rows as satisfied in validation sign-off when green.
    - Note: first green GitHub Actions run may require push to remote — document "pending remote CI" if runner unavailable locally.
    GSD validation doc may live on planning branch per D-15; product workflow on claude/lighttune-main.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-shared-test-strategy-ci/03-VALIDATION.md && grep -q 'TST-01' .planning/phases/03-shared-test-strategy-ci/03-VALIDATION.md && grep -q 'TST-04' .planning/phases/03-shared-test-strategy-ci/03-VALIDATION.md && lua5.4 tests/run.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - 03-VALIDATION.md exists with per-plan task map
    - All four TST requirements referenced in sign-off gates
    - Local lua5.4 tests/run.lua and pytest both pass at validation time
    - Phase 3 ROADMAP success criteria 1–4 marked TRUE in validation doc
    - Wave dependency chain 03-01 → 03-02 → 03-03 documented complete
  </acceptance_criteria>
  <done>Phase 3 complete — shared test strategy and CI gate ready for Phase 4 bridge thinning and Phase 5 HTTP integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo code | Ephemeral ubuntu-latest; checkout only; no production secrets |
| CI → show network / Pi | No connection — mock-only bridge tests |
| Public CI logs | Test output may show mock meter readings — no operator IPs or config.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-06 | Tampering | Malicious PR bypassing CI | medium | mitigate | Branch protection recommended outside Phase 3 scope; workflow runs on PR to cursor/** and main |
| T-3-07 | Information disclosure | CI log leakage | low | accept | No secrets in workflow; D-53 enforced |
| T-3-08 | Denial of service | pip/apt supply chain | low | accept | Pin dev deps in requirements-dev.txt; use official actions checkout/setup-python |

</threat_model>

<verification>
- .github/workflows/ci.yml with host-lua and bridge-pytest jobs
- README CI section updated
- 03-VALIDATION.md with TST-01–04 sign-off
- Local parity: lua5.4 tests/run.lua && pytest tests/test_bridge_routes.py both exit 0
</verification>

<success_criteria>
- TST-04: CI workflow runs host Lua tests and bridge pytest on push
- Phase 3 ROADMAP success criteria 1–4 all TRUE
- TST-01, TST-02, TST-03 verified via validation gate (delivered in Waves 1–2, signed off Wave 3)
- Ready for Phase 4 thin bridge and Phase 5 MA3 HTTP integration
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Location |
|----------|------|----------|
| GitHub Actions CI workflow | YAML | .github/workflows/ci.yml |
| host-lua job | CI job | .github/workflows/ci.yml |
| bridge-pytest job | CI job | .github/workflows/ci.yml |
| README CI documentation | doc section | README.md |
| Phase 3 validation sign-off | doc | .planning/phases/03-shared-test-strategy-ci/03-VALIDATION.md |

<output>
Create `.planning/phases/03-shared-test-strategy-ci/03-03-SUMMARY.md` when done
</output>

## PLANNING COMPLETE
