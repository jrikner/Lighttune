---
phase: 03-shared-test-strategy-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run.lua
  - tests/lib_assert.lua
  - tests/test_color_math.lua
  - tests/test_fixture_db.lua
  - tests/test_goals.lua
  - test_color_math.lua
  - README.md
autonomous: true
requirements:
  - TST-01
must_haves:
  truths:
    - tests/run.lua is the single host entry point; sets package.path to ./lua/?.lua and exits non-zero on any FAIL (D-40, D-44)
    - Domain tests split into tests/test_color_math.lua, tests/test_fixture_db.lua, tests/test_goals.lua — each require shared modules from lua/ (D-41)
    - Root test_color_math.lua is a thin backward-compat wrapper delegating to tests/run.lua — no duplicated assertion logic (D-42, D-34)
    - lua5.4 tests/run.lua preserves 133+ PASS with zero FAIL (D-43 floor 126+, regression target 133+)
    - Shared harness (assert_near, assert_equal, section, PASS/FAIL counters) lives in one place — not copied per test file
  artifacts:
    - tests/run.lua
    - tests/lib_assert.lua
    - tests/test_color_math.lua
    - tests/test_fixture_db.lua
    - tests/test_goals.lua
    - test_color_math.lua
  key_links:
    - from: tests/run.lua
      to: tests/test_*.lua
      via: dofile or require with tests/ on package.path
      pattern: test_color_math|test_fixture_db|test_goals
    - from: tests/test_color_math.lua
      to: lua/color_math.lua
      via: require("color_math")
      pattern: color_math\.cct_to_xy
    - from: test_color_math.lua
      to: tests/run.lua
      via: dofile("tests/run.lua") or equivalent delegate
      pattern: tests/run\.lua
---

<objective>
Create a unified host test runner under `tests/` and split the monolithic `test_color_math.lua` into domain-scoped test modules while keeping the repo-root wrapper for README backward compatibility.

Purpose: Satisfies TST-01 (host tests via `lua5.4` against shared modules with 126+ assertions preserved) and establishes the layout Wave 2 fixtures and Wave 3 CI will invoke.
Output: `tests/run.lua` entry point; three domain test files; shared assertion library; thin root wrapper; README updated with new run command.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md
@.planning/phases/02-plugin-hardening-test-seams/02-03-SUMMARY.md
@.planning/codebase/TESTING.md
@test_color_math.lua
@lua/color_math.lua
@lua/fixture_db.lua
@lua/goals.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared assertion harness (tests/lib_assert.lua)</name>
  <files>tests/lib_assert.lua</files>
  <read_first>test_color_math.lua, .planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md, .planning/codebase/TESTING.md</read_first>
  <action>
    Per D-40 and TESTING.md patterns: create tests/lib_assert.lua exporting a return-M table with assert_near (default tolerance 0.0005), assert_equal, assert_true, assert_false, section(name), and PASS/FAIL counter accessors (get_counts, reset, or module-level M.PASS/M.FAIL mutated by helpers). Preserve exact output format from current test_color_math.lua ("  PASS  label", "  FAIL  label", "[section name]" banners) so CI log diffs stay readable. No MA3 globals. Module must be require-able after tests/run.lua prepends tests/?.lua to package.path.
  </action>
  <verify>
    <automated>test -f tests/lib_assert.lua && grep -q 'return M' tests/lib_assert.lua && grep -q 'assert_near' tests/lib_assert.lua && grep -q 'section' tests/lib_assert.lua</automated>
  </verify>
  <acceptance_criteria>
    - tests/lib_assert.lua exists with return M export table
    - assert_near, assert_equal, assert_true, assert_false, and section present
    - PASS/FAIL counting mechanism exposed for run.lua summary
    - Output string format matches existing test_color_math.lua conventions
  </acceptance_criteria>
  <done>Shared assertion library ready for domain test modules — no per-file harness duplication.</done>
</task>

<task type="auto">
  <name>Task 2: Split monolithic tests into domain modules (TST-01, D-41)</name>
  <files>tests/test_color_math.lua, tests/test_fixture_db.lua, tests/test_goals.lua</files>
  <read_first>test_color_math.lua, lua/color_math.lua, lua/fixture_db.lua, lua/goals.lua, .planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md</read_first>
  <action>
    Per D-41: migrate content from root test_color_math.lua into three files under tests/:
    - tests/test_color_math.lua — all color_math sections (cct_to_xy, xy_to_uvp, apply_duv_correction, rgb/hsb, rate_quality, rate_duv, gel_hint); require("color_math") and require("lib_assert") or local assert = require("lib_assert").
    - tests/test_fixture_db.lua — JSON encode/parse, best_* flags, recompute_best_flags, append_fixture_record, sort_fixture_records, find_best_for_fixture; require("fixture_db").
    - tests/test_goals.lua — goals_met boundary cases from Phase 2 Wave 3; require("goals").
    Each file exports a run(M) or run_all(assert) function returning nothing but mutating shared PASS/FAIL via lib_assert. Move section banners verbatim — do not delete or weaken assertions. Preserve deterministic date literals (e.g. "2026-01-01") in fixture_db tests. Do NOT add bridge or plugin tests (Phase 5). Remove any stale base64 test sections if still present — base64 was deleted in Phase 2.
  </action>
  <verify>
    <automated>test -f tests/test_color_math.lua && test -f tests/test_fixture_db.lua && test -f tests/test_goals.lua && grep -q 'require("color_math")' tests/test_color_math.lua && grep -q 'require("fixture_db")' tests/test_fixture_db.lua && grep -q 'require("goals")' tests/test_goals.lua</automated>
  </verify>
  <acceptance_criteria>
    - Three domain test files exist under tests/
    - Each file requires the correct lua/ shared module
    - Color-math sections from original test_color_math.lua present in tests/test_color_math.lua
    - Fixture DB and goals sections present in respective files
    - No inline duplicate of Section 2/2b function bodies — require-only pattern
    - Combined assertion count ≥ 133 (document count in commit message)
  </acceptance_criteria>
  <done>Domain tests organized by module boundary matching Phase 2 extraction (color_math, fixture_db, goals).</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/run.lua entry point and root wrapper (TST-01, D-40, D-42, D-44)</name>
  <files>tests/run.lua, test_color_math.lua, README.md</files>
  <read_first>test_color_math.lua, .planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md, README.md</read_first>
  <action>
    Per D-40, D-42, D-44, D-54: create tests/run.lua that (1) prepends package.path with "./lua/?.lua" and "./tests/?.lua" relative to repo root (detect via debug.getinfo or assume cwd is repo root — document assumption in header comment); (2) requires lib_assert and resets counters; (3) invokes tests/test_color_math.lua, tests/test_fixture_db.lua, tests/test_goals.lua in fixed order via dofile or exported run functions; (4) prints summary "Results: N passed, M failed" matching current format; (5) calls os.exit(1) when FAIL > 0. Replace root test_color_math.lua with thin wrapper (~10 lines): header comment noting deprecation path, package.path bootstrap if needed, dofile("tests/run.lua") — MUST NOT duplicate harness or test bodies (D-34). Update README.md: primary command `lua5.4 tests/run.lua`; note root `lua5.4 test_color_math.lua` still works as alias. Run full suite locally: lua5.4 tests/run.lua must show 133+ PASS, 0 FAIL. Commit product changes to claude/lighttune-main per D-54.
  </action>
  <verify>
    <automated>command -v lua5.4 && lua5.4 tests/run.lua; test $? -eq 0 && lua5.4 test_color_math.lua; test $? -eq 0 && wc -l test_color_math.lua | awk '{print $1}' | xargs test 30 -gt</automated>
  </verify>
  <acceptance_criteria>
    - tests/run.lua exists and is documented as single host entry point
    - lua5.4 tests/run.lua exits 0 with 133+ PASS and zero FAIL
    - lua5.4 test_color_math.lua exits 0 (backward compat)
    - Root test_color_math.lua is thin wrapper (&lt; 30 lines) with no duplicated test logic
    - README documents tests/run.lua as primary command
    - os.exit(1) verified when a deliberate FAIL is injected then reverted (manual or comment in summary)
  </acceptance_criteria>
  <done>TST-01 satisfied — unified host runner executes against shared modules with assertion baseline preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host test runner → lua/ modules | Local filesystem only; no network; no secrets |
| CI consumer → tests/run.lua | Future Wave 3 will invoke same entry point — path assumptions must be cwd=repo-root |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering | package.path resolution | low | mitigate | run.lua documents cwd=repo-root; CI job uses `working-directory: .` |
| T-3-02 | Denial of service | Test split regression | medium | mitigate | D-43: gate on 133+ PASS; Wave 3 CI enforces exit code |

</threat_model>

<verification>
- tests/run.lua, tests/lib_assert.lua, tests/test_*.lua exist
- lua5.4 tests/run.lua exits 0 with 133+ PASS
- lua5.4 test_color_math.lua backward-compat exits 0
- Root wrapper has no duplicated harness or test bodies
- README updated with new run command
</verification>

<success_criteria>
- TST-01: Host tests run via lua5.4 against shared modules; 126+ assertions preserved (133+ target)
- tests/ directory established as canonical host test layout for Waves 2–3
- Zero FAIL in full suite after split
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Location |
|----------|------|----------|
| Host test runner | Lua script | tests/run.lua |
| Shared assertion library | Lua module (`return M`) | tests/lib_assert.lua |
| Color math domain tests | Lua test module | tests/test_color_math.lua |
| Fixture DB domain tests | Lua test module | tests/test_fixture_db.lua |
| Goals domain tests | Lua test module | tests/test_goals.lua |
| Backward-compat wrapper | thin Lua script | test_color_math.lua (repo root) |
| Updated run instructions | doc section | README.md |

<output>
Create `.planning/phases/03-shared-test-strategy-ci/03-01-SUMMARY.md` when done
</output>

## PLANNING COMPLETE
