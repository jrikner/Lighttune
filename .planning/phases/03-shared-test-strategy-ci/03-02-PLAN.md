---
phase: 03-shared-test-strategy-ci
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - tests/fixtures/bridge_status_ok.json
  - tests/fixtures/bridge_measure_ok.json
  - tests/fixtures/bridge_discover_mock.json
  - tests/fixtures/fixture_db_quote_make.json
  - tests/test_bridge_routes.py
  - tests/conftest.py
  - sekonic-bridge/requirements-dev.txt
  - tests/test_fixture_db.lua
autonomous: true
requirements:
  - TST-02
  - TST-03
must_haves:
  truths:
    - Golden JSON fixtures live under tests/fixtures/ covering bridge /status, /measure, /discover mock shapes and fixture DB quote-in-make edge case (D-48)
    - Bridge route tests use pytest + httpx against FastAPI app with --mock meter — no USB hardware (D-45, D-46)
    - GET /status, POST /measure, GET /discover are tested; /capture and /learn_trigger are NOT CI gates (D-47)
    - Fixture comparisons validate key fields and types — not dynamic uptime_s or measure timestamps (D-49)
    - pytest exits 0 locally with all bridge route tests green
  artifacts:
    - tests/fixtures/bridge_status_ok.json
    - tests/fixtures/bridge_measure_ok.json
    - tests/fixtures/bridge_discover_mock.json
    - tests/fixtures/fixture_db_quote_make.json
    - tests/test_bridge_routes.py
    - sekonic-bridge/requirements-dev.txt
  key_links:
    - from: tests/test_bridge_routes.py
      to: sekonic-bridge/server.py
      via: FastAPI TestClient or httpx with mock meter patched
      pattern: /status|/measure|/discover
    - from: tests/test_bridge_routes.py
      to: tests/fixtures/*.json
      via: json.load + field/type assertions
      pattern: bridge_status_ok|bridge_measure_ok|bridge_discover_mock
    - from: tests/test_fixture_db.lua
      to: tests/fixtures/fixture_db_quote_make.json
      via: golden record roundtrip or parse assertion
      pattern: fixture_db_quote_make
---

<objective>
Add golden JSON fixtures for bridge MeasurementRecord responses and fixture DB edge cases, then implement pytest/httpx route tests against the mock meter backend for `/status`, `/measure`, and `/discover`.

Purpose: Satisfies TST-02 (bridge route tests via pytest/httpx against mock meter) and TST-03 (golden JSON fixtures for bridge responses and fixture DB edge cases). Builds on Wave 1 host test layout; Wave 3 CI will invoke the same pytest suite.
Output: Four golden fixture files; pytest suite with conftest; sekonic-bridge dev requirements file; optional fixture_db golden test wired in Lua suite.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md
@.planning/codebase/TESTING.md
@sekonic-bridge/server.py
@sekonic-bridge/meter_mock.py
@tests/run.lua
@lua/fixture_db.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create golden JSON fixtures (TST-03, D-48, D-49)</name>
  <files>tests/fixtures/bridge_status_ok.json, tests/fixtures/bridge_measure_ok.json, tests/fixtures/bridge_discover_mock.json, tests/fixtures/fixture_db_quote_make.json</files>
  <read_first>.planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md, sekonic-bridge/server.py, sekonic-bridge/meter_mock.py, lua/fixture_db.lua</read_first>
  <action>
    Per D-48, D-49: create tests/fixtures/ directory with four golden files:
    - bridge_status_ok.json — expected /status shape in mock mode: status "ok", meter "C-7000", connected true, version string, device_configured/protocol_captured/trigger_discovered true; omit or use placeholder comments for uptime_s and last_error (tests compare types not exact values).
    - bridge_measure_ok.json — MeasurementRecord contract: cct (int), duv (float), cri (int), r9 (int), tlci (int optional); values representative of mock first-call progression (~4100K, +0.0085 Duv) or documented stable subset.
    - bridge_discover_mock.json — mock /discover response: configured true, manufacturer "Sekonic", product contains "mock", vendor_id/product_id strings, devices array (may be empty).
    - fixture_db_quote_make.json — single fixture_log record with make containing embedded double-quote (e.g. Acme "Pro" 600) plus required fields (model, kelvin, date, contributor, cct, duv, cri, r9); documents DB-01 edge case from Phase 2 hardening.
    Add tests/fixtures/README.md one-liner optional — only if needed for field semantics; prefer self-documenting JSON keys. Do NOT commit runtime device_config.json or bridge.log.
  </action>
  <verify>
    <automated>test -f tests/fixtures/bridge_status_ok.json && test -f tests/fixtures/bridge_measure_ok.json && test -f tests/fixtures/bridge_discover_mock.json && test -f tests/fixtures/fixture_db_quote_make.json && python3 -c "import json; [json.load(open(f)) for f in ['tests/fixtures/bridge_status_ok.json','tests/fixtures/bridge_measure_ok.json','tests/fixtures/bridge_discover_mock.json','tests/fixtures/fixture_db_quote_make.json']]"</automated>
  </verify>
  <acceptance_criteria>
    - All four golden JSON files exist under tests/fixtures/
    - Each file is valid JSON parseable by Python json and usable from Lua tests
    - bridge_measure_ok.json includes cct, duv, cri, r9 keys with correct JSON types
    - fixture_db_quote_make.json includes quoted make string edge case
    - Dynamic fields (uptime_s) documented as excluded from exact-match assertions
  </acceptance_criteria>
  <done>TST-03 fixture inventory committed — bridge and fixture DB golden references available to host and Python tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire fixture_db golden test in Lua suite (TST-03)</name>
  <files>tests/test_fixture_db.lua</files>
  <read_first>tests/test_fixture_db.lua, tests/fixtures/fixture_db_quote_make.json, lua/fixture_db.lua, .planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md</read_first>
  <action>
    Per D-48: extend tests/test_fixture_db.lua with a [golden fixture_db_quote_make] section that loads tests/fixtures/fixture_db_quote_make.json. Implementation options (executor discretion): minimal Lua JSON field extraction for make/model only, or inline table mirroring golden file with comment linking to fixture path. Assert json_encode_db_record → json_parse_db_array roundtrip preserves quoted make; skipped == 0. Ensures TST-03 fixture DB edge case is exercised by host runner (tests/run.lua), not only Python tests. Re-run lua5.4 tests/run.lua — still 133+ PASS, zero FAIL.
  </action>
  <verify>
    <automated>grep -q 'fixture_db_quote_make' tests/test_fixture_db.lua && lua5.4 tests/run.lua; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_fixture_db.lua references golden fixture_db_quote_make.json
    - Quoted make roundtrip test passes under tests/run.lua
    - lua5.4 tests/run.lua exits 0
    - No regression in total PASS count
  </acceptance_criteria>
  <done>Fixture DB golden edge case covered in host Lua suite per TST-03.</done>
</task>

<task type="auto">
  <name>Task 3: Add pytest bridge route tests with mock meter (TST-02, D-45–D-47)</name>
  <files>tests/test_bridge_routes.py, tests/conftest.py, sekonic-bridge/requirements-dev.txt</files>
  <read_first>sekonic-bridge/server.py, sekonic-bridge/meter_mock.py, tests/fixtures/*.json, .planning/phases/03-shared-test-strategy-ci/03-CONTEXT.md, .planning/codebase/TESTING.md</read_first>
  <action>
    Per D-45, D-46, D-47 and TESTING.md recommended layout: create sekonic-bridge/requirements-dev.txt pinning pytest and httpx (versions compatible with FastAPI 0.115.0 / Python 3.12). Add tests/conftest.py fixture that imports sekonic-bridge/server.py app with mock meter enabled — prefer FastAPI TestClient (starlette.testclient) if lifespan and _use_mock_global work without subprocess; else pytest fixture starts uvicorn subprocess on ephemeral port with --mock. Create tests/test_bridge_routes.py with async or sync tests:
    - test_status_mock — GET /status returns 200; assert keys status, meter, connected, version match bridge_status_ok.json types and static values; uptime_s is int ≥ 0 (not exact golden match).
    - test_measure_mock — POST /measure returns 200; body has cct, duv, cri, r9; types match bridge_measure_ok.json; optional tlci key allowed.
    - test_discover_mock — GET /discover returns 200; configured true; product/manufacturer match bridge_discover_mock.json static fields.
    Do NOT add /capture or /learn_trigger as required passing tests. Run locally: `cd /workspace && python3 -m venv .venv && .venv/bin/pip install -r sekonic-bridge/requirements.txt -r sekonic-bridge/requirements-dev.txt && .venv/bin/pytest tests/test_bridge_routes.py -v`. Commit to claude/lighttune-main per D-54.
  </action>
  <verify>
    <automated>test -f sekonic-bridge/requirements-dev.txt && test -f tests/test_bridge_routes.py && grep -q 'pytest' sekonic-bridge/requirements-dev.txt && grep -q '/status' tests/test_bridge_routes.py && grep -q '/measure' tests/test_bridge_routes.py && grep -q '/discover' tests/test_bridge_routes.py</automated>
  </verify>
  <acceptance_criteria>
    - sekonic-bridge/requirements-dev.txt lists pytest and httpx (runtime requirements.txt unchanged for Pi)
    - tests/test_bridge_routes.py tests GET /status, POST /measure, GET /discover
    - Tests run against mock meter — no USB/pyusb hardware required
    - Golden fixtures used for static field/type assertions per D-49
    - pytest tests/test_bridge_routes.py exits 0 locally
    - /capture and /learn_trigger not in required test list
  </acceptance_criteria>
  <done>TST-02 satisfied — bridge routes verified via pytest/httpx against mock backend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest → FastAPI app | In-process TestClient or localhost only; no external network |
| Mock meter → /measure response | Simulated data; not trusted for production calibration accuracy |
| Golden fixtures → assertions | Read-only reference data; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-03 | Spoofing | Unauthenticated /measure in tests | low | accept | Tests document same trust model as production Phase 1–3; MTR-08 auth deferred Phase 4 |
| T-3-04 | Tampering | Flaky uptime_s assertions | low | mitigate | D-49: compare types/ranges not exact uptime |
| T-3-05 | Denial of service | Mock measure 1.5s sleep | low | accept | pytest tolerates MockMeter sleep; CI timeout ≥ 60s per test module |

</threat_model>

<verification>
- Four golden JSON files under tests/fixtures/
- fixture_db golden referenced in tests/test_fixture_db.lua
- pytest tests/test_bridge_routes.py exits 0
- lua5.4 tests/run.lua still exits 0
- requirements-dev.txt separate from Pi runtime requirements.txt
</verification>

<success_criteria>
- TST-02: Bridge /status, /measure, /discover pass pytest against mock meter
- TST-03: Golden JSON fixtures cover bridge responses and fixture DB quote-in-make edge case
- Host and Python test suites both green before Wave 3 CI wiring
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Location |
|----------|------|----------|
| bridge_status_ok.json | golden fixture | tests/fixtures/ |
| bridge_measure_ok.json | golden fixture | tests/fixtures/ |
| bridge_discover_mock.json | golden fixture | tests/fixtures/ |
| fixture_db_quote_make.json | golden fixture | tests/fixtures/ |
| Bridge route pytest suite | Python tests | tests/test_bridge_routes.py |
| pytest fixtures (mock app) | conftest | tests/conftest.py |
| Dev test dependencies | requirements | sekonic-bridge/requirements-dev.txt |
| Golden fixture_db Lua test | test case | tests/test_fixture_db.lua |

<output>
Create `.planning/phases/03-shared-test-strategy-ci/03-02-SUMMARY.md` when done
</output>

## PLANNING COMPLETE
