# GSD Core local Cursor install (run: npx @opengsd/gsd-core@latest --cursor --local)
.cursor/

# Never commit operator config (plugin root, same folder as plugin.xml)
config.json

# Local measurement logs
data/fixture_log.json
data/measurements/*.json
